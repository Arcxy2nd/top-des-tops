{
  "name": "top-des-tops",
  "version": "1.0.0",
  "private": true,
  "description": "Suite de tests de la logique métier du backend Google Apps Script (Code.gs).",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  }
}
