# Audit Log Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un journal d'audit centralisé : toute opération mutante écrit une ligne dans une feuille `AuditLog`, visible dans un second sous-onglet de l'onglet Historique.

**Architecture:** `AuditService` (IIFE dans `Code.gs`) expose une seule méthode publique `log(author, action, entity, before, after, detail)` — silencieuse en cas d'erreur, toujours appelée dans un `withLock()`. Les 26 fonctions `api*` mutantes reçoivent un paramètre `author` en dernière position et appellent `AuditService.log()` après leur mutation. Le frontend passe `_whoAmI || ''` à tous les `callServer` mutants. L'onglet Historique adopte la structure sous-onglets avec des classes dédiées `history-*` (pas `settings-*`) pour éviter les conflits CSS/JS.

**Tech Stack:** Google Apps Script (ES6 subset), HTML/CSS/JS vanilla, Node.js test harness (`node:test` + VM sandbox `tests/harness.js`). `Code.gs` et `Index.html` restent **monolithiques** (contrainte GAS — pas de split de fichiers).

---

## Contraintes projet à respecter

- `Code.gs` et `Index.html` restent en fichiers uniques — pas de split
- Tests via `node --check Code.gs` (syntaxe) et `npm test` (harness Node VM)
- Avatar obligatoire partout où un nom de joueur apparaît dans l'UI
- Zéro perte de fonctionnalité existante

---

## Task 1 — AuditService + ConfigService + tests fondation

**Files:**
- Modify: `Code.gs` (lignes 31–34 + insertion après ligne 93)
- Modify: `tests/harness.js` (ligne 105 — exports)
- Create: `tests/audit.test.js`

- [ ] **Étape 1 : Ajouter `auditLog` au cache `ConfigService.getSheets()`**

Dans `Code.gs`, repérer ces 4 lignes (actuellement ~lignes 31–34) :

```javascript
const notes   = ss.getSheetByName('Notes')   || null;
const bareme  = ss.getSheetByName('Bareme')  || null;
const phrases = ss.getSheetByName('Phrases') || null;
_cache = { spreadsheet: ss, history, players, categories, notes, bareme, phrases };
```

Les remplacer par :

```javascript
const notes    = ss.getSheetByName('Notes')    || null;
const bareme   = ss.getSheetByName('Bareme')   || null;
const phrases  = ss.getSheetByName('Phrases')  || null;
const auditLog = ss.getSheetByName('AuditLog') || null;
_cache = { spreadsheet: ss, history, players, categories, notes, bareme, phrases, auditLog };
```

- [ ] **Étape 2 : Insérer `AuditService` dans `Code.gs`**

Repérer la ligne `// ─── SETTINGS SERVICE` (~ligne 95). Insérer le bloc suivant JUSTE AVANT :

```javascript
// ─── AUDIT SERVICE ─────────────────────────────────────────────────────────────
const AuditService = (() => {
  /** Auto-creates the AuditLog sheet if absent (same lazy pattern as Notes/Bareme). */
  function _getOrCreateSheet() {
    const cache = ConfigService.getSheets();
    if (cache.auditLog) return cache.auditLog;
    const sheet = cache.spreadsheet.insertSheet('AuditLog');
    sheet.appendRow(['Timestamp', 'Auteur', 'Action', 'Entité', 'Avant', 'Après', 'Détail']);
    sheet.getRange(1, 1, 1, 7).setFontWeight('bold');
    ConfigService.clearCache();
    return ConfigService.getSheets().auditLog;
  }

  /**
   * Appends one audit row. Never throws — audit failure must not break the caller.
   * Must be called inside a withLock() block (lock is already held by the caller).
   * author : _whoAmI from frontend | action : human-readable label
   * entity : affected resource | before/after : readable diff | detail : extra context
   */
  function log(author, action, entity, before, after, detail) {
    try {
      _getOrCreateSheet().appendRow([
        new Date(),
        author  || '',
        action  || '',
        entity  || '',
        before  || '',
        after   || '',
        detail  || ''
      ]);
    } catch (_) {}
  }

  return { log };
})();
```

- [ ] **Étape 3 : Vérifier la syntaxe**

```bash
node --check Code.gs
```

Expected: no output (exit 0).

- [ ] **Étape 4 : Mettre à jour `tests/harness.js` — exporter `AuditService` et `apiGetAuditLog`**

Repérer les lignes 105–108 dans `tests/harness.js` :

```javascript
const epilogue = '\n;this.__exports = { ConfigService, SettingsService, StorageService, ' +
  'NotesService, AnalyticsService, BaremeService, PhrasesService, withLock, ' +
  'apiDetectDistributedLots, apiAddBulkPlan, apiUpdateHistoryEntry, ' +
  'apiFixZeroPoints, apiDeleteOrphans, apiUpdateBulkEntries };';
```

Remplacer par :

```javascript
const epilogue = '\n;this.__exports = { ConfigService, AuditService, SettingsService, StorageService, ' +
  'NotesService, AnalyticsService, BaremeService, PhrasesService, withLock, ' +
  'apiDetectDistributedLots, apiAddBulkPlan, apiUpdateHistoryEntry, ' +
  'apiGetAuditLog, apiFixZeroPoints, apiDeleteOrphans, apiUpdateBulkEntries };';
```

- [ ] **Étape 5 : Créer `tests/audit.test.js`**

```javascript
'use strict';
const { test } = require('node:test');
const assert   = require('assert');
const { loadGas, makeSheet, injectSheets } = require('./harness.js');

function makeAuditSheet() {
  return makeSheet([['Timestamp','Auteur','Action','Entité','Avant','Après','Détail']]);
}

function withAuditSheets(gas, auditSheet) {
  injectSheets(gas, {
    spreadsheet: { insertSheet: () => auditSheet, getSheetByName: () => null },
    history: makeSheet([]), players: makeSheet([]), categories: makeSheet([]),
    notes: null, bareme: null, phrases: null, auditLog: auditSheet
  });
}

// ─── AuditService.log ───────────────────────────────────────────────────────────

test('AuditService.log appends one row with all fields', () => {
  const gas   = loadGas();
  const audit = makeAuditSheet();
  withAuditSheets(gas, audit);

  gas.AuditService.log('Alice', 'Saisie de points', 'History', '', '3 entrées', '2026-01-10');

  assert.strictEqual(audit._grid.length, 2);
  const row = audit._grid[1];
  assert.ok(row[0] instanceof Date, 'timestamp is a Date');
  assert.strictEqual(row[1], 'Alice');
  assert.strictEqual(row[2], 'Saisie de points');
  assert.strictEqual(row[3], 'History');
  assert.strictEqual(row[4], '');
  assert.strictEqual(row[5], '3 entrées');
  assert.strictEqual(row[6], '2026-01-10');
});

test('AuditService.log replaces null/undefined args with empty strings', () => {
  const gas   = loadGas();
  const audit = makeAuditSheet();
  withAuditSheets(gas, audit);

  gas.AuditService.log(null, 'Action', undefined, null, null, null);
  const row = audit._grid[1];
  assert.strictEqual(row[1], '');
  assert.strictEqual(row[3], '');
  assert.strictEqual(row[4], '');
});

test('AuditService.log is completely silent when appendRow throws', () => {
  const gas = loadGas();
  const broken = { ...makeAuditSheet(), appendRow() { throw new Error('Sheet quota exceeded'); } };
  withAuditSheets(gas, broken);
  assert.doesNotThrow(() => gas.AuditService.log('Alice', 'Test', '', '', '', ''));
});

test('AuditService.log auto-creates AuditLog sheet when absent', () => {
  const gas     = loadGas();
  const newSheet = makeAuditSheet();
  let created   = false;
  injectSheets(gas, {
    spreadsheet: { insertSheet: () => { created = true; return newSheet; }, getSheetByName: () => null },
    history: makeSheet([]), players: makeSheet([]), categories: makeSheet([]),
    notes: null, bareme: null, phrases: null, auditLog: null
  });
  gas.AuditService.log('Bob', 'Test', '', '', '', '');
  assert.ok(created, 'insertSheet was called to create AuditLog');
  assert.strictEqual(newSheet._grid.length, 2);
});
```

- [ ] **Étape 6 : Lancer les tests**

```bash
npm test -- --test-name-pattern "AuditService"
```

Expected: 4 tests PASS.

- [ ] **Étape 7 : Commit**

```bash
git add Code.gs tests/harness.js tests/audit.test.js
git commit -m "feat: add AuditService + update ConfigService cache + test foundation"
```

---

## Task 2 — `apiGetAuditLog` : endpoint de lecture paginé et filtrable

**Files:**
- Modify: `Code.gs` (ajouter à la fin du fichier, après `apiSetActivePhrasePreset`)
- Modify: `tests/audit.test.js` (ajouter des tests)

- [ ] **Étape 1 : Ajouter `apiGetAuditLog` à la fin de `Code.gs`**

Ajouter après la dernière fonction `apiSetActivePhrasePreset` :

```javascript
// ── Journal d'audit (lecture paginée et filtrable) ─────────────────────────────
function apiGetAuditLog(page, pageSize, filterAuthor, filterAction, startDate, endDate) {
  try {
    const sheet = ConfigService.getSheets().auditLog;
    if (!sheet) return { success: true, logs: [], total: 0 };
    const lastRow = sheet.getLastRow();
    if (lastRow <= 1) return { success: true, logs: [], total: 0 };

    const data  = sheet.getRange(2, 1, lastRow - 1, 7).getValues();
    const start = startDate ? new Date(startDate + 'T00:00:00') : null;
    const end   = endDate   ? new Date(endDate   + 'T23:59:59') : null;

    const filtered = [];
    for (let i = data.length - 1; i >= 0; i--) {  // reverse → les plus récents d'abord
      const row = data[i];
      const ts  = new Date(row[0]);
      if (isNaN(ts.getTime())) continue;
      if (filterAuthor && row[1] !== filterAuthor) continue;
      if (filterAction && row[2] !== filterAction) continue;
      if (start && ts < start) continue;
      if (end   && ts > end)   continue;
      filtered.push({
        timestamp: ts.toISOString(),
        author:    row[1] ? row[1].toString() : '',
        action:    row[2] ? row[2].toString() : '',
        entity:    row[3] ? row[3].toString() : '',
        before:    row[4] ? row[4].toString() : '',
        after:     row[5] ? row[5].toString() : '',
        detail:    row[6] ? row[6].toString() : ''
      });
    }

    const total  = filtered.length;
    const ps     = parseInt(pageSize, 10) || 20;
    const offset = ((parseInt(page, 10) || 1) - 1) * ps;
    return { success: true, logs: filtered.slice(offset, offset + ps), total };
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 2 : Vérifier la syntaxe**

```bash
node --check Code.gs
```

Expected: no output.

- [ ] **Étape 3 : Ajouter les tests `apiGetAuditLog` dans `tests/audit.test.js`**

```javascript
// ─── apiGetAuditLog ─────────────────────────────────────────────────────────────

function makeFilledAuditSheet(rows) {
  // rows: [[author, action, entity, before, after, detail], ...]
  const grid = [['Timestamp','Auteur','Action','Entité','Avant','Après','Détail']];
  rows.forEach((r, i) => {
    const d = new Date('2026-01-' + String(i + 1).padStart(2, '0'));
    grid.push([d, r[0], r[1], r[2], r[3], r[4], r[5]]);
  });
  return makeSheet(grid);
}

test('apiGetAuditLog returns empty result when auditLog sheet is absent', () => {
  const gas = loadGas();
  injectSheets(gas, {
    spreadsheet: { insertSheet: () => makeAuditSheet(), getSheetByName: () => null },
    history: makeSheet([]), players: makeSheet([]), categories: makeSheet([]),
    notes: null, bareme: null, phrases: null, auditLog: null
  });
  const res = gas.apiGetAuditLog(1, 20, null, null, null, null);
  assert.strictEqual(res.success, true);
  assert.deepStrictEqual(res.logs, []);
  assert.strictEqual(res.total, 0);
});

test('apiGetAuditLog returns rows in reverse order (most recent first)', () => {
  const gas   = loadGas();
  const audit = makeFilledAuditSheet([
    ['Alice', 'Action A', '', '', '', ''],
    ['Bob',   'Action B', '', '', '', '']
  ]);
  injectSheets(gas, {
    spreadsheet: { insertSheet: () => audit, getSheetByName: () => null },
    history: makeSheet([]), players: makeSheet([]), categories: makeSheet([]),
    notes: null, bareme: null, phrases: null, auditLog: audit
  });
  const res = gas.apiGetAuditLog(1, 20, null, null, null, null);
  assert.strictEqual(res.logs[0].author, 'Bob');    // row 2 (latest) comes first
  assert.strictEqual(res.logs[1].author, 'Alice');
});

test('apiGetAuditLog filters by author', () => {
  const gas   = loadGas();
  const audit = makeFilledAuditSheet([
    ['Alice', 'Action', '', '', '', ''],
    ['Bob',   'Action', '', '', '', ''],
    ['Alice', 'Action', '', '', '', '']
  ]);
  injectSheets(gas, {
    spreadsheet: { insertSheet: () => audit, getSheetByName: () => null },
    history: makeSheet([]), players: makeSheet([]), categories: makeSheet([]),
    notes: null, bareme: null, phrases: null, auditLog: audit
  });
  const res = gas.apiGetAuditLog(1, 20, 'Alice', null, null, null);
  assert.strictEqual(res.total, 2);
  assert.ok(res.logs.every(l => l.author === 'Alice'));
});

test('apiGetAuditLog filters by action', () => {
  const gas   = loadGas();
  const audit = makeFilledAuditSheet([
    ['Alice', 'Saisie de points',  '', '', '', ''],
    ['Bob',   'Suppression entrée','', '', '', '']
  ]);
  injectSheets(gas, {
    spreadsheet: { insertSheet: () => audit, getSheetByName: () => null },
    history: makeSheet([]), players: makeSheet([]), categories: makeSheet([]),
    notes: null, bareme: null, phrases: null, auditLog: audit
  });
  const res = gas.apiGetAuditLog(1, 20, null, 'Saisie de points', null, null);
  assert.strictEqual(res.total, 1);
  assert.strictEqual(res.logs[0].action, 'Saisie de points');
});

test('apiGetAuditLog paginates correctly (25 rows, pageSize 20)', () => {
  const gas   = loadGas();
  const rows  = Array.from({ length: 25 }, (_, i) => ['Alice', 'Action ' + i, '', '', '', '']);
  const audit = makeFilledAuditSheet(rows);
  injectSheets(gas, {
    spreadsheet: { insertSheet: () => audit, getSheetByName: () => null },
    history: makeSheet([]), players: makeSheet([]), categories: makeSheet([]),
    notes: null, bareme: null, phrases: null, auditLog: audit
  });
  const page1 = gas.apiGetAuditLog(1, 20, null, null, null, null);
  const page2 = gas.apiGetAuditLog(2, 20, null, null, null, null);
  assert.strictEqual(page1.logs.length, 20);
  assert.strictEqual(page2.logs.length, 5);
  assert.strictEqual(page1.total, 25);
  assert.strictEqual(page2.total, 25);
});
```

- [ ] **Étape 4 : Lancer les tests**

```bash
npm test -- --test-name-pattern "apiGetAuditLog"
```

Expected: 5 tests PASS.

- [ ] **Étape 5 : Commit**

```bash
git add Code.gs tests/audit.test.js
git commit -m "feat: add apiGetAuditLog with pagination and author/action/date filters"
```

---

## Task 3 — Helpers before-state + intégration History mutations (12 fonctions)

**Files:**
- Modify: `Code.gs` (insertion de helpers + remplacement de 12 fonctions api*)

Les helpers `_*Summary()` lisent l'état d'une ligne AVANT la mutation pour la colonne `Avant` du log. Ils sont insérés juste avant les fonctions api* qui les utilisent et encapsulent toutes les erreurs.

- [ ] **Étape 1 : Insérer les helpers privés juste avant `apiDeleteHistoryEntry` dans `Code.gs`**

Repérer `function apiDeleteHistoryEntry(rowIndex)` et insérer le bloc suivant JUSTE AVANT :

```javascript
// ─── AUDIT BEFORE-STATE HELPERS ────────────────────────────────────────────────
// Read current state before destructive mutations so the audit log can record
// a human-readable "before". Every function swallows errors — a bad rowIndex
// must never prevent the main operation from completing.

function _historyRowSummary(rowIndex) {
  try {
    const row = ConfigService.getSheets().history.getRange(rowIndex, 1, 1, 5).getValues()[0];
    const d   = new Date(row[0]);
    const ds  = isNaN(d.getTime()) ? '?'
      : d.getDate() + '/' + (d.getMonth() + 1) + '/' + d.getFullYear();
    return [row[1] || '?', row[2] || '?', (parseInt(row[3], 10) || '?') + ' pts', ds, row[4] || ''].join(' | ');
  } catch (_) { return 'ligne #' + rowIndex; }
}

function _historyDescSummary(rowIndex) {
  try {
    return ConfigService.getSheets().history.getRange(rowIndex, 5).getValue().toString();
  } catch (_) { return ''; }
}

function _noteRowSummary(rowIndex) {
  try {
    const sheet = ConfigService.getSheets().notes;
    if (!sheet) return '';
    const row = sheet.getRange(rowIndex, 1, 1, 3).getValues()[0];
    return (row[1] || '') + ' : ' + (row[2] || '');
  } catch (_) { return ''; }
}

function _baremeRowSummary(rowIndex) {
  try {
    const sheet = ConfigService.getSheets().bareme;
    if (!sheet) return '';
    const row = sheet.getRange(rowIndex, 1, 1, 3).getValues()[0];
    return [row[0] || '', row[1] || '', String(row[2] || 0) + ' pts'].join(' | ');
  } catch (_) { return ''; }
}

function _phraseRowSummary(rowIndex) {
  try {
    const sheet = ConfigService.getSheets().phrases;
    if (!sheet) return '';
    const row = sheet.getRange(rowIndex, 1, 1, 3).getValues()[0];
    return '[' + (row[1] || '') + '] ' + (row[2] || '') + ' (preset: ' + (row[0] || '') + ')';
  } catch (_) { return ''; }
}

function _entitySummary(type, name) {
  try {
    const found = SettingsService.getEntities(type).find(function(e) { return e.name === name; });
    if (!found) return name;
    if (type === 'Players')
      return name + ' (avatar: ' + (found.meta || '') + ', couleur: ' + (found.color || '') + ')';
    return name + ' (' + (found.meta || '') + ', ' + (found.icon || '') + ', ' + (found.color || '') + ')';
  } catch (_) { return name; }
}

function _entityColorSummary(type, name) {
  try {
    const found = SettingsService.getEntities(type).find(function(e) { return e.name === name; });
    return found ? (found.color || '') : '';
  } catch (_) { return ''; }
}
```

- [ ] **Étape 2 : Remplacer `apiAddBulkPlan`**

```javascript
function apiAddBulkPlan(plan, author) {
  try {
    return withLock(() => {
      StorageService.appendBulkPlan(plan);
      const totalEntries = plan.reduce(function(s, d) { return s + (d.entries || []).length; }, 0);
      const firstDate    = plan[0] && plan[0].date ? plan[0].date : '';
      AuditService.log(author, 'Saisie de points', 'History', '', totalEntries + ' entrée(s)',
        firstDate ? 'à partir du ' + firstDate : '');
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 3 : Remplacer `apiDeleteHistoryEntry`**

```javascript
function apiDeleteHistoryEntry(rowIndex, author) {
  try {
    return withLock(() => {
      const before = _historyRowSummary(rowIndex);
      StorageService.deleteHistoryEntry(rowIndex);
      AuditService.log(author, 'Suppression entrée', 'History', before, '', 'ligne #' + rowIndex);
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 4 : Remplacer `apiDeleteHistoryEntries`**

```javascript
function apiDeleteHistoryEntries(rowIndexes, author) {
  try {
    return withLock(() => {
      const { history } = ConfigService.getSheets();
      const sorted = [...rowIndexes].sort((a, b) => b - a);
      sorted.forEach(ri => history.deleteRow(ri));
      ConfigService.clearCache();
      AuditService.log(author, 'Suppression bulk', 'History', '', '', rowIndexes.length + ' entrée(s)');
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 5 : Remplacer `apiUpdateHistoryDescription`**

```javascript
function apiUpdateHistoryDescription(rowIndex, description, author) {
  try {
    return withLock(() => {
      const before = _historyDescSummary(rowIndex);
      StorageService.updateHistoryDescription(rowIndex, description);
      AuditService.log(author, 'Description modifiée', 'History', before, description || '', 'ligne #' + rowIndex);
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 6 : Remplacer `apiUpdateHistoryEntry`**

```javascript
function apiUpdateHistoryEntry(rowIndex, fields, author) {
  try {
    return withLock(() => {
      const before = _historyRowSummary(rowIndex);
      StorageService.updateHistoryEntry(rowIndex, fields);
      const after = [fields.player || '?', fields.category || '?',
        (parseInt(fields.points, 10) || '?') + ' pts', fields.date || '',
        fields.description || ''].join(' | ');
      AuditService.log(author, 'Modification entrée', 'History', before, after, 'ligne #' + rowIndex);
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 7 : Remplacer `apiUpdateBulkDescription`**

```javascript
function apiUpdateBulkDescription(rowIndexes, description, author) {
  try {
    if (!rowIndexes || !rowIndexes.length) throw new Error("Aucune ligne sélectionnée.");
    return withLock(() => {
      const { history } = ConfigService.getSheets();
      const lastRow = history.getLastRow();
      if (lastRow <= 1) return { success: true };
      const colRange = history.getRange(2, 5, lastRow - 1, 1);
      const values   = colRange.getValues();
      const indexSet = new Set(rowIndexes.map(ri => parseInt(ri, 10)));
      for (let i = 0; i < values.length; i++) {
        if (indexSet.has(i + 2)) values[i][0] = description || '';
      }
      colRange.setValues(values);
      AuditService.log(author, 'Description bulk', 'History', '', description || '',
        rowIndexes.length + ' entrée(s)');
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 8 : Remplacer `apiUpdateBulkEntries`**

```javascript
function apiUpdateBulkEntries(rowIndexes, partialFields, author) {
  try {
    if (!rowIndexes || !rowIndexes.length) throw new Error("Aucune ligne sélectionnée.");
    if (!partialFields || !Object.keys(partialFields).length) return { success: true };
    return withLock(function() {
      var history  = ConfigService.getSheets().history;
      var lastRow  = history.getLastRow();
      if (lastRow <= 1) return { success: true, skipped: [] };

      var allData  = history.getRange(2, 1, lastRow - 1, 7).getValues();
      var indexSet = new Set(rowIndexes.map(function(ri) { return parseInt(ri, 10); }));
      var skipped  = [];

      var hasDate   = 'date'        in partialFields;
      var hasPlayer = 'player'      in partialFields;
      var hasCat    = 'category'    in partialFields;
      var hasPts    = 'points'      in partialFields;
      var hasDesc   = 'description' in partialFields;
      var hasSais   = 'saiseur'     in partialFields;

      indexSet.forEach(function(idx) {
        var rowI = idx - 2;
        if (rowI < 0 || rowI >= allData.length) { skipped.push(idx); return; }
        var row      = allData[rowI];
        var player   = hasPlayer ? partialFields.player   : (row[1] ? row[1].toString() : '');
        var category = hasCat    ? partialFields.category : (row[2] ? row[2].toString() : '');
        var pts      = hasPts    ? parseInt(partialFields.points, 10) : parseInt(row[3], 10);
        var desc     = hasDesc   ? (partialFields.description || '') : (row[4] ? row[4].toString() : '');
        var saiseur  = hasSais   ? (partialFields.saiseur  || '') : (row[6] ? row[6].toString() : '');

        if (!player || !category || isNaN(pts) || pts < 1) { skipped.push(idx); return; }

        var targetDate;
        if (hasDate) {
          targetDate = new Date(partialFields.date + 'T12:00:00');
          if (isNaN(targetDate.getTime())) { skipped.push(idx); return; }
        } else {
          targetDate = (row[0] instanceof Date) ? row[0] : new Date(row[0]);
        }

        history.getRange(idx, 1, 1, 5).setValues([[targetDate, player, category, pts, desc]]);
        if (hasSais) history.getRange(idx, 7).setValue(saiseur);
      });

      var changedFields = Object.keys(partialFields).join(', ');
      AuditService.log(author, 'Modification bulk', 'History', '', changedFields,
        (rowIndexes.length - skipped.length) + ' entrée(s) modifiée(s)');
      ConfigService.clearCache();
      return { success: true, skipped: skipped };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 9 : Remplacer `apiFixZeroPoints`**

```javascript
function apiFixZeroPoints(author) {
  try {
    return withLock(() => {
      const result = StorageService.fixZeroPoints();
      AuditService.log(author, 'Nettoyage zéros', 'History', '', '',
        result.deleted + ' entrée(s) supprimée(s)');
      ConfigService.clearCache();
      return { success: true, deleted: result.deleted };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 10 : Remplacer `apiDeleteOrphans`**

```javascript
function apiDeleteOrphans(author) {
  try {
    return withLock(() => {
      const result = StorageService.deleteOrphans();
      AuditService.log(author, 'Nettoyage orphelins', 'History', '', '',
        result.deleted + ' entrée(s) supprimée(s)');
      ConfigService.clearCache();
      return { success: true, deleted: result.deleted };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 11 : Remplacer `apiGroupRows`**

```javascript
function apiGroupRows(rowIndexes, author) {
  try {
    if (!rowIndexes || rowIndexes.length < 2) throw new Error("Sélectionnez au moins 2 entrées.");
    return withLock(() => {
      const { history } = ConfigService.getSheets();
      const lastRow = history.getLastRow();
      if (lastRow <= 1) throw new Error("Historique vide.");
      const gid      = 'G' + Date.now() + '_' + Math.random().toString(36).substr(2, 5);
      const colRange = history.getRange(2, 6, lastRow - 1, 1);
      const values   = colRange.getValues();
      const indexSet = new Set(rowIndexes.map(ri => parseInt(ri, 10)));
      for (let i = 0; i < values.length; i++) {
        if (indexSet.has(i + 2)) values[i][0] = gid;
      }
      colRange.setValues(values);
      AuditService.log(author, 'Groupement lot', 'History', '', '',
        rowIndexes.length + ' entrée(s), gid: ' + gid);
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 12 : Remplacer `apiUngroupLot`**

```javascript
function apiUngroupLot(groupId, author) {
  try {
    if (!groupId) throw new Error("GroupID manquant.");
    return withLock(() => {
      const sheet = ConfigService.getSheets().history;
      const lastRow = sheet.getLastRow();
      if (lastRow <= 1) return { success: true };
      const data = sheet.getRange(2, 6, lastRow - 1, 1).getValues();
      for (var i = 0; i < data.length; i++) {
        if (data[i][0] && data[i][0].toString() === groupId) {
          sheet.getRange(i + 2, 6).setValue('');
        }
      }
      AuditService.log(author, 'Dégroupement lot', 'History', groupId, '', '');
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 13 : Remplacer `apiGroupDistributedLots`**

```javascript
function apiGroupDistributedLots(lotsToGroup, author) {
  try {
    if (!lotsToGroup || !lotsToGroup.length) throw new Error("Aucun lot fourni.");
    return withLock(() => {
      const sheet = ConfigService.getSheets().history;
      lotsToGroup.forEach(function(lot) {
        var rows = lot.rowIndexes;
        if (!rows || rows.length < 2) return;
        var gid = 'G' + Date.now() + '_' + Math.random().toString(36).substr(2, 5);
        rows.forEach(function(r) { sheet.getRange(r, 6).setValue(gid); });
      });
      AuditService.log(author, 'Lots auto-groupés', 'History', '', '',
        lotsToGroup.length + ' lot(s)');
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 14 : Vérifier la syntaxe**

```bash
node --check Code.gs
```

Expected: no output.

- [ ] **Étape 15 : Lancer tous les tests**

```bash
npm test
```

Expected: all tests PASS (y compris ceux des Tasks 1 et 2).

- [ ] **Étape 16 : Commit**

```bash
git add Code.gs
git commit -m "feat: integrate AuditService into 12 History api* functions + before-state helpers"
```

---

## Task 4 — Intégration Settings, Notes, Barème, Phrases (14 fonctions)

**Files:**
- Modify: `Code.gs` (remplacement de 14 fonctions api*)

- [ ] **Étape 1 : Remplacer `apiSetColor`**

```javascript
function apiSetColor(type, name, color, author) {
  try {
    if (!SettingsService.VALID_TYPES.includes(type)) throw new Error("Type invalide.");
    return withLock(() => {
      const before = _entityColorSummary(type, name);
      SettingsService.setEntityColor(type, name, color);
      const label = type === 'Players' ? 'Joueur' : 'Top';
      AuditService.log(author, 'Couleur ' + label.toLowerCase(), label + ': ' + name,
        before, color || '', '');
      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 2 : Remplacer `apiManageEntity`**

```javascript
function apiManageEntity(action, type, newName, newMeta, oldName, newIcon, author) {
  try {
    if (!SettingsService.VALID_TYPES.includes(type))     throw new Error("Type invalide.");
    if (!SettingsService.VALID_ACTIONS.includes(action)) throw new Error("Action invalide.");
    return withLock(() => {
      const label = type === 'Players' ? 'Joueur' : 'Top';

      if (action === 'ADD') {
        SettingsService.addEntity(type, newName, newMeta, newIcon);
        const after = type === 'Players'
          ? (newName || '') + ' (avatar: ' + (newMeta || '') + ')'
          : (newName || '') + ' (' + (newMeta || '') + ', ' + (newIcon || '') + ')';
        AuditService.log(author, label + ' ajouté', label + ': ' + (newName || ''), '', after, '');
      }
      if (action === 'DELETE') {
        const before = _entitySummary(type, oldName);
        SettingsService.deleteEntity(type, oldName);
        AuditService.log(author, label + ' supprimé', label + ': ' + (oldName || ''), before, '', '');
      }
      if (action === 'RENAME') {
        SettingsService.renameEntity(type, oldName, newName, newMeta, newIcon);
        AuditService.log(author, label + ' renommé', label + ': ' + (oldName || ''),
          oldName || '', newName || '', '');
      }

      ConfigService.clearCache();
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 3 : Remplacer `apiAddNote`, `apiDeleteNote`, `apiEditNote`**

```javascript
function apiAddNote(player, text, dateStr, author) {
  try {
    return withLock(() => {
      NotesService.addNote(player, text, dateStr);
      AuditService.log(author, 'Note ajoutée', 'Note: ' + (player || ''),
        '', (player || '') + ' : ' + (text || '').trim(), '');
      return { success: true };
    });
  } catch(e) { return fail(e); }
}

function apiDeleteNote(rowIndex, author) {
  try {
    return withLock(() => {
      const before = _noteRowSummary(rowIndex);
      NotesService.deleteNote(rowIndex);
      AuditService.log(author, 'Note supprimée', 'Note', before, '', 'ligne #' + rowIndex);
      return { success: true };
    });
  } catch(e) { return fail(e); }
}

function apiEditNote(rowIndex, newText, author) {
  try {
    return withLock(() => {
      const before = _noteRowSummary(rowIndex);
      NotesService.editNote(rowIndex, newText);
      AuditService.log(author, 'Note modifiée', 'Note', before, (newText || '').trim(),
        'ligne #' + rowIndex);
      return { success: true };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 4 : Remplacer `apiAddBaremeEntry`, `apiUpdateBaremeEntry`, `apiDeleteBaremeEntry`**

```javascript
function apiAddBaremeEntry(top, action, pts, author) {
  try {
    return withLock(() => {
      BaremeService.addEntry(top, action, pts);
      const after = [top || '', action || '', String(Number(pts) || 0) + ' pts'].join(' | ');
      AuditService.log(author, 'Règle ajoutée', 'Barème', '', after, '');
      ConfigService.clearCache();
      return { success: true, entries: BaremeService.getEntries() };
    });
  } catch(e) { return fail(e); }
}

function apiUpdateBaremeEntry(rowIndex, action, pts, author) {
  try {
    return withLock(() => {
      const before = _baremeRowSummary(rowIndex);
      BaremeService.updateEntry(rowIndex, action, pts);
      const after = (action || '') + ' | ' + String(Number(pts) || 0) + ' pts';
      AuditService.log(author, 'Règle modifiée', 'Barème', before, after, 'ligne #' + rowIndex);
      ConfigService.clearCache();
      return { success: true, entries: BaremeService.getEntries() };
    });
  } catch(e) { return fail(e); }
}

function apiDeleteBaremeEntry(rowIndex, author) {
  try {
    return withLock(() => {
      const before = _baremeRowSummary(rowIndex);
      BaremeService.deleteEntry(rowIndex);
      AuditService.log(author, 'Règle supprimée', 'Barème', before, '', 'ligne #' + rowIndex);
      ConfigService.clearCache();
      return { success: true, entries: BaremeService.getEntries() };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 5 : Remplacer les 6 fonctions Phrases**

```javascript
function apiAddPhrase(preset, pool, text, author) {
  try {
    return withLock(() => {
      PhrasesService.addPhrase(preset, pool, text);
      const after = '[' + (pool || '') + '] ' + (text || '').trim() + ' (preset: ' + (preset || '') + ')';
      AuditService.log(author, 'Phrase ajoutée', 'Phrases: ' + (preset || ''), '', after, '');
      ConfigService.clearCache();
      return { success: true, phrases: PhrasesService.getAll() };
    });
  } catch(e) { return fail(e); }
}

function apiSavePhrasesBatch(entries, author) {
  try {
    return withLock(() => {
      PhrasesService.saveBatch(entries);
      const preset = entries && entries.length ? entries[0].preset : '';
      AuditService.log(author, 'Phrases batch', 'Phrases: ' + (preset || ''), '', '',
        (entries || []).length + ' phrase(s)');
      ConfigService.clearCache();
      return { success: true, phrases: PhrasesService.getAll() };
    });
  } catch(e) { return fail(e); }
}

function apiUpdatePhrase(rowIndex, text, author) {
  try {
    return withLock(() => {
      const before = _phraseRowSummary(rowIndex);
      PhrasesService.updatePhrase(rowIndex, text);
      AuditService.log(author, 'Phrase modifiée', 'Phrases', before, (text || '').trim(),
        'ligne #' + rowIndex);
      ConfigService.clearCache();
      return { success: true, phrases: PhrasesService.getAll() };
    });
  } catch(e) { return fail(e); }
}

function apiDeletePhrase(rowIndex, author) {
  try {
    return withLock(() => {
      const before = _phraseRowSummary(rowIndex);
      PhrasesService.deletePhrase(rowIndex);
      AuditService.log(author, 'Phrase supprimée', 'Phrases', before, '', 'ligne #' + rowIndex);
      ConfigService.clearCache();
      return { success: true, phrases: PhrasesService.getAll() };
    });
  } catch(e) { return fail(e); }
}

function apiDeletePreset(presetName, author) {
  try {
    return withLock(() => {
      PhrasesService.deletePreset(presetName);
      AuditService.log(author, 'Preset supprimé', 'Phrases: ' + (presetName || ''), '', '', '');
      ConfigService.clearCache();
      return { success: true, phrases: PhrasesService.getAll() };
    });
  } catch(e) { return fail(e); }
}

function apiRenamePreset(oldName, newName, author) {
  try {
    if (!newName || !newName.trim()) throw new Error("Nouveau nom vide.");
    if (oldName === newName.trim()) return { success: true, phrases: PhrasesService.getAll() };
    return withLock(() => {
      const sheet = ConfigService.getSheets().phrases;
      if (!sheet) throw new Error("Feuille Phrases introuvable.");
      const lastRow = sheet.getLastRow();
      if (lastRow <= 1) return { success: true, phrases: [] };
      const data = sheet.getRange(2, 1, lastRow - 1, 1).getValues();
      let modified = false;
      for (let i = 0; i < data.length; i++) {
        if (data[i][0].toString() === oldName) { data[i][0] = newName.trim(); modified = true; }
      }
      if (modified) sheet.getRange(2, 1, lastRow - 1, 1).setValues(data);
      AuditService.log(author, 'Preset renommé', 'Phrases', oldName || '', newName.trim(), '');
      ConfigService.clearCache();
      return { success: true, phrases: PhrasesService.getAll() };
    });
  } catch(e) { return fail(e); }
}
```

- [ ] **Étape 6 : Vérifier la syntaxe**

```bash
node --check Code.gs
```

Expected: no output.

- [ ] **Étape 7 : Lancer tous les tests**

```bash
npm test
```

Expected: all tests PASS.

- [ ] **Étape 8 : Commit**

```bash
git add Code.gs
git commit -m "feat: integrate AuditService into Settings/Notes/Bareme/Phrases mutations (14 functions)"
```

---

## Task 5 — Frontend HTML + CSS : sous-onglets Historique

**Files:**
- Modify: `Index.html` (CSS + HTML tab-history)

- [ ] **Étape 1 : Ajouter le CSS des sous-onglets Historique + tableau audit**

Repérer `/* ── ENTITY ITEMS — redesign ── */` (~ligne 1702) dans `Index.html`. Insérer JUSTE AVANT :

```css
  /* ── HISTORIQUE : SOUS-ONGLETS ── */
  .history-inner-nav {
    display: flex; gap: 4px; flex-wrap: wrap;
    background: rgba(0,0,0,0.2); padding: 4px; border-radius: 14px;
    margin-bottom: 20px;
  }
  .history-nav-btn {
    flex: 1; min-width: 80px; padding: 10px 8px;
    border: none; border-radius: 11px; cursor: pointer;
    font-size: 0.82rem; font-weight: 600; white-space: nowrap;
    background: transparent; color: var(--text-muted);
    transition: background 0.18s, color 0.18s;
  }
  .history-nav-btn.active {
    background: var(--card); color: var(--text);
    box-shadow: 0 2px 8px rgba(0,0,0,0.22);
  }
  .history-nav-btn:hover:not(.active) { color: var(--text); background: rgba(255,255,255,0.05); }
  .history-tab-pane { display: none; }
  .history-tab-pane.active { display: block; }

  /* ── AUDIT LOG ── */
  .audit-before-after { display: flex; align-items: center; gap: 6px; font-size: 0.8rem; flex-wrap: wrap; }
  .audit-before { color: var(--error); text-decoration: line-through; opacity: 0.85; }
  .audit-after  { color: var(--success); }
  .audit-arrow  { color: var(--text-muted); flex-shrink: 0; }
  .audit-filters {
    display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 14px; align-items: center;
  }
  .audit-filters select, .audit-filters input[type="date"] {
    width: auto; flex: 1; min-width: 120px;
  }
  @media (max-width: 640px) {
    .history-nav-btn { font-size: 0.75rem; padding: 9px 5px; }
  }
```

- [ ] **Étape 2 : Remplacer le HTML de `#tab-history`**

Repérer le bloc `<!-- ══ HISTORIQUE ════ -->` (~ligne 2061) et remplacer l'intégralité du `<div id="tab-history" ...>` (jusqu'à son `</div>` de fermeture, ~ligne 2098) par :

```html
  <!-- ══ HISTORIQUE ════════════════════════════════════════════════════ -->
  <div id="tab-history" class="tab-content">

    <!-- Navigation interne -->
    <div class="history-inner-nav">
      <button class="history-nav-btn active" data-stab="stab-history-entries">📜 Entrées</button>
      <button class="history-nav-btn" data-stab="stab-history-audit">🔍 Journal</button>
    </div>

    <!-- ─ Entrées ─ (contenu existant inchangé) -->
    <div id="stab-history-entries" class="history-tab-pane active">
      <div class="card">
        <h2>📜 Historique des entrées</h2>
        <div class="history-filters">
          <input type="text" id="historyTextFilter" placeholder="🔍 Rechercher…" style="flex:1; min-width:140px; max-width:240px;">
          <button id="refreshHistoryBtn" class="secondary small">🔄</button>
          <button id="histSelectBtn" class="secondary small">☑ Sélectionner</button>
        </div>
        <div class="hist-filter-section">
          <div class="hist-filter-row">
            <span class="hist-filter-label">Joueurs</span>
            <div class="hist-fchip-row" id="histPlayerChips"></div>
          </div>
          <div class="hist-filter-row">
            <span class="hist-filter-label">Tops</span>
            <div class="hist-fchip-row" id="histCategoryChips"></div>
          </div>
        </div>
        <div class="table-wrap">
          <table>
            <thead>
              <tr><th id="histSelAllTh" class="hist-sel-th" style="display:none;"></th><th>Date</th><th>Joueur</th><th>Top</th><th>Pts</th><th>Saiseur</th><th></th></tr>
            </thead>
            <tbody id="historyTableBody"></tbody>
          </table>
        </div>
        <div id="historyPagination" class="pagination"></div>
        <div id="histBulkBar" class="hist-bulk-bar" style="display:none;">
          <span id="histBulkCount">0 sélectionné(s)</span>
          <button id="histBulkEdit" class="secondary">✏️ Modifier la sélection</button>
          <button id="histBulkSelectAll" class="secondary small">Tout sélectionner</button>
          <button id="histBulkGroup" class="secondary small">🔗 Grouper</button>
          <button id="histBulkDelete" class="danger small">🗑️ Supprimer</button>
          <button id="histBulkCancel" class="secondary small">✕ Annuler</button>
        </div>
      </div>
    </div>

    <!-- ─ Journal d'audit ─ (nouveau) -->
    <div id="stab-history-audit" class="history-tab-pane">
      <div class="card">
        <h2>🔍 Journal d'audit</h2>
        <div class="audit-filters">
          <select id="auditFilterAuthor">
            <option value="">Tous les auteurs</option>
          </select>
          <select id="auditFilterAction">
            <option value="">Toutes les actions</option>
            <option>Saisie de points</option>
            <option>Suppression entrée</option>
            <option>Suppression bulk</option>
            <option>Modification entrée</option>
            <option>Description modifiée</option>
            <option>Description bulk</option>
            <option>Modification bulk</option>
            <option>Groupement lot</option>
            <option>Lots auto-groupés</option>
            <option>Dégroupement lot</option>
            <option>Nettoyage zéros</option>
            <option>Nettoyage orphelins</option>
            <option>Joueur ajouté</option>
            <option>Joueur supprimé</option>
            <option>Joueur renommé</option>
            <option>Couleur joueur</option>
            <option>Top ajouté</option>
            <option>Top supprimé</option>
            <option>Top renommé</option>
            <option>Couleur top</option>
            <option>Note ajoutée</option>
            <option>Note supprimée</option>
            <option>Note modifiée</option>
            <option>Règle ajoutée</option>
            <option>Règle modifiée</option>
            <option>Règle supprimée</option>
            <option>Phrase ajoutée</option>
            <option>Phrases batch</option>
            <option>Phrase modifiée</option>
            <option>Phrase supprimée</option>
            <option>Preset supprimé</option>
            <option>Preset renommé</option>
          </select>
          <input type="date" id="auditFilterFrom" title="Depuis">
          <input type="date" id="auditFilterTo"   title="Jusqu'au">
          <button id="auditRefreshBtn" class="secondary small">🔄 Actualiser</button>
        </div>
        <div class="table-wrap">
          <table>
            <thead>
              <tr>
                <th>Quand</th><th>Qui</th><th>Action</th>
                <th>Entité</th><th>Avant → Après</th><th>Détail</th>
              </tr>
            </thead>
            <tbody id="auditTableBody"></tbody>
          </table>
        </div>
        <div id="auditPagination" class="pagination"></div>
      </div>
    </div>

  </div>
```

- [ ] **Étape 3 : Vérifier visuellement (ouvrir Index.html en local ou via GAS preview)**

Confirmer :
- L'onglet Historique affiche deux boutons de sous-navigation ("📜 Entrées" actif, "🔍 Journal")
- Le contenu existant de l'historique est intact dans le sous-onglet "Entrées"
- Le sous-onglet "Journal" affiche le tableau + filtres (vide — le JS n'est pas encore branché)

- [ ] **Étape 4 : Commit**

```bash
git add Index.html
git commit -m "feat: add History sub-tabs HTML/CSS — Entrées + Journal d'audit"
```

---

## Task 6 — Frontend JS : `initHistoryTabs`, `loadAuditLog`, `renderAuditTable`

**Files:**
- Modify: `Index.html` (section `<script>`)

- [ ] **Étape 1 : Ajouter les variables d'état audit log**

Repérer `let _whoAmI = localStorage.getItem(WHO_AM_I_KEY) || null;` (~ligne 2317). Juste après ce bloc de variables globales, ajouter :

```javascript
  // ── Audit log state ──────────────────────────────────────────────────────────
  let _auditCurrentPage = 1;
  const AUDIT_PAGE_SIZE = 20;
```

- [ ] **Étape 2 : Ajouter `initHistoryTabs()` juste après `initSettingsTabs()`**

Repérer `function initSettingsTabs()` (~ligne 2858) et sa fermeture `}`. Insérer APRÈS :

```javascript
  // ── HISTORIQUE : SOUS-ONGLETS ────────────────────────────────────────────────
  function initHistoryTabs() {
    const container = document.getElementById('tab-history');
    container.querySelectorAll('.history-nav-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const target = btn.dataset.stab;
        container.querySelectorAll('.history-nav-btn').forEach(b =>
          b.classList.toggle('active', b === btn));
        container.querySelectorAll('.history-tab-pane').forEach(p =>
          p.classList.toggle('active', p.id === target));
        if (target === 'stab-history-audit') loadAuditLog(1);
      });
    });
  }
```

- [ ] **Étape 3 : Ajouter `loadAuditLog` et `renderAuditTable` dans la section `// ── HISTORIQUE`**

Repérer `// ── HISTORIQUE` (~ligne 6085) et insérer les deux fonctions AVANT `loadHistoryPage` :

```javascript
  // ── JOURNAL D'AUDIT ──────────────────────────────────────────────────────────

  function loadAuditLog(page) {
    _auditCurrentPage = page || 1;
    const author = document.getElementById('auditFilterAuthor').value || null;
    const action = document.getElementById('auditFilterAction').value || null;
    const from   = document.getElementById('auditFilterFrom').value  || null;
    const to     = document.getElementById('auditFilterTo').value    || null;
    callServer(
      'apiGetAuditLog',
      [_auditCurrentPage, AUDIT_PAGE_SIZE, author, action, from, to],
      res => renderAuditTable(res),
      'Chargement journal'
    );
  }

  function renderAuditTable(res) {
    const tbody = document.getElementById('auditTableBody');
    const pag   = document.getElementById('auditPagination');
    tbody.innerHTML = '';
    pag.innerHTML   = '';

    if (!res || !res.success || !res.logs || !res.logs.length) {
      const empty = tbody.insertRow();
      const td = empty.insertCell();
      td.colSpan = 6;
      td.style.cssText = 'text-align:center;color:var(--text-muted);padding:24px;';
      td.textContent = 'Aucune entrée dans le journal.';
      return;
    }

    const pad2 = n => String(n).padStart(2, '0');

    res.logs.forEach(log => {
      const tr = tbody.insertRow();

      // Quand
      const ts     = new Date(log.timestamp);
      const dateStr = isNaN(ts.getTime()) ? '—'
        : ts.getDate() + '/' + pad2(ts.getMonth() + 1) + '/' + ts.getFullYear()
          + ' ' + pad2(ts.getHours()) + ':' + pad2(ts.getMinutes());
      const tdWhen = tr.insertCell();
      tdWhen.textContent = dateStr;
      tdWhen.style.cssText = 'font-size:0.78rem;color:var(--text-muted);white-space:nowrap;';

      // Qui (avatar + nom — règle UX universelle)
      const tdWho = tr.insertCell();
      tdWho.className = 'hist-player-cell';
      if (log.author) {
        const p    = cachedPlayers.find(pl => pl.name === log.author);
        const img  = document.createElement('img');
        img.src    = getAvatarUrl(log.author, p ? p.meta : '');
        img.onerror = () => { img.src = getAvatarUrl(log.author, ''); };
        img.style.cssText = 'width:22px;height:22px;border-radius:50%;object-fit:cover;flex-shrink:0;';
        const span = document.createElement('span');
        span.className = 'hist-player-name';
        span.textContent = log.author;
        tdWho.appendChild(img);
        tdWho.appendChild(span);
      } else {
        tdWho.textContent = '—';
      }

      // Action
      const tdAction = tr.insertCell();
      tdAction.textContent = log.action || '—';
      tdAction.style.fontWeight = '600';

      // Entité
      const tdEntity = tr.insertCell();
      tdEntity.textContent = log.entity || '—';
      tdEntity.style.cssText = 'font-size:0.82rem;color:var(--text-muted);';

      // Avant → Après
      const tdDiff = tr.insertCell();
      if (log.before && log.after) {
        const wrap = document.createElement('div');
        wrap.className = 'audit-before-after';
        const bef = document.createElement('span'); bef.className = 'audit-before'; bef.textContent = log.before;
        const arr = document.createElement('span'); arr.className = 'audit-arrow';  arr.textContent = '→';
        const aft = document.createElement('span'); aft.className = 'audit-after';  aft.textContent = log.after;
        wrap.append(bef, arr, aft);
        tdDiff.appendChild(wrap);
      } else if (log.before) {
        const s = document.createElement('span'); s.className = 'audit-before'; s.textContent = log.before;
        tdDiff.appendChild(s);
      } else if (log.after) {
        const s = document.createElement('span'); s.className = 'audit-after'; s.textContent = log.after;
        tdDiff.appendChild(s);
      } else {
        tdDiff.textContent = '—';
      }

      // Détail
      const tdDetail = tr.insertCell();
      tdDetail.textContent = log.detail || '';
      tdDetail.style.cssText = 'font-size:0.78rem;color:var(--text-muted);';
    });

    // Pagination
    const totalPages = Math.ceil(res.total / AUDIT_PAGE_SIZE);
    if (totalPages > 1) {
      const prev = document.createElement('button');
      prev.textContent = '←'; prev.className = 'secondary small';
      prev.disabled    = _auditCurrentPage <= 1;
      prev.onclick     = () => loadAuditLog(_auditCurrentPage - 1);

      const info = document.createElement('span');
      info.textContent = 'Page ' + _auditCurrentPage + ' / ' + totalPages;
      info.style.cssText = 'font-size:0.82rem;color:var(--text-muted);';

      const next = document.createElement('button');
      next.textContent = '→'; next.className = 'secondary small';
      next.disabled    = _auditCurrentPage >= totalPages;
      next.onclick     = () => loadAuditLog(_auditCurrentPage + 1);

      pag.append(prev, info, next);
    }
  }
```

- [ ] **Étape 4 : Appeler `initHistoryTabs()` dans le bloc d'initialisation**

Chercher `initSettingsTabs();` dans le bloc d'initialisation (~ligne 7555). Juste après, ajouter :

```javascript
      initHistoryTabs();
```

- [ ] **Étape 5 : Mettre à jour `auditFilterAuthor` après le chargement des joueurs**

Chercher la fonction (ou le bloc) qui remplit `cachedPlayers` suite à `apiGetSettings`. Chercher un pattern `cachedPlayers = res.players` (ou `= res.players || []`). Juste après cette ligne, ajouter :

```javascript
        const auSel = document.getElementById('auditFilterAuthor');
        if (auSel) {
          while (auSel.options.length > 1) auSel.remove(1);
          (cachedPlayers || []).forEach(p => {
            const opt = document.createElement('option');
            opt.value = p.name; opt.textContent = p.name;
            auSel.appendChild(opt);
          });
        }
```

- [ ] **Étape 6 : Brancher les event listeners des filtres audit**

Repérer `document.getElementById('refreshHistoryBtn').addEventListener(...)` (~ligne 6214). Dans le même bloc d'initialisation, ajouter après :

```javascript
    document.getElementById('auditRefreshBtn').addEventListener('click',   () => loadAuditLog(1));
    document.getElementById('auditFilterAuthor').addEventListener('change', () => loadAuditLog(1));
    document.getElementById('auditFilterAction').addEventListener('change', () => loadAuditLog(1));
    document.getElementById('auditFilterFrom').addEventListener('change',   () => loadAuditLog(1));
    document.getElementById('auditFilterTo').addEventListener('change',     () => loadAuditLog(1));
```

- [ ] **Étape 7 : Vérifier la syntaxe**

```bash
node --check Code.gs
```

Expected: no output.

- [ ] **Étape 8 : Commit**

```bash
git add Index.html
git commit -m "feat: add audit log JS (initHistoryTabs, loadAuditLog, renderAuditTable)"
```

---

## Task 7 — Frontend : passer `_whoAmI` dans tous les `callServer` mutants

**Files:**
- Modify: `Index.html` (~35 call sites dans le `<script>`)

**Pattern uniforme** — pour chaque `callServer` mutant :
```javascript
// AVANT :  callServer('apiFoo', [arg1, arg2], onSuccess, label)
// APRÈS :  callServer('apiFoo', [arg1, arg2, _whoAmI || ''], onSuccess, label)
```

Pour les fonctions sans arguments (`apiFixZeroPoints`, `apiDeleteOrphans`) :
```javascript
// AVANT :  callServer('apiFixZeroPoints', [], onSuccess, label)
// APRÈS :  callServer('apiFixZeroPoints', [_whoAmI || ''], onSuccess, label)
```

- [ ] **Étape 1 : Mettre à jour les 9 call sites Phrases**

| Ligne approx. | Fonction | Paramètre à ajouter |
|---|---|---|
| ~2625 | `apiAddPhrase` | `_whoAmI \|\| ''` après `txt` |
| ~2661 | `apiDeletePhrase` | `_whoAmI \|\| ''` après `entry.rowIndex` |
| ~2717 | `apiUpdatePhrase` | `_whoAmI \|\| ''` après `text` |
| ~2719 | `apiAddPhrase` (2e occurrence) | `_whoAmI \|\| ''` après `text` |
| ~2794 | `apiSavePhrasesBatch` | `_whoAmI \|\| ''` après `entries` |
| ~2806 | `apiDeletePreset` | `_whoAmI \|\| ''` après `active` |
| ~2846 | `apiRenamePreset` | `_whoAmI \|\| ''` après `newName` |
| ~2899 | `apiSavePhrasesBatch` (2e occurrence) | `_whoAmI \|\| ''` après `entries` |
| ~7566 | `apiSavePhrasesBatch` (seed au 1er lancement) | `_whoAmI \|\| ''` après `seed` |

- [ ] **Étape 2 : Mettre à jour les 7 call sites Settings**

| Ligne approx. | Fonction + Action | Paramètre à ajouter |
|---|---|---|
| ~3976 | `apiSetColor` (color picker inline-save) | `_whoAmI \|\| ''` après `col` |
| ~3977 | `apiManageEntity` RENAME (color picker) | `_whoAmI \|\| ''` en 7e position du tableau |
| ~4112 | `apiSetColor` Players | `_whoAmI \|\| ''` après `colorIn.value` |
| ~4144 | `apiSetColor` Categories | `_whoAmI \|\| ''` après `colorIn.value` |
| ~4177 | `apiManageEntity` DELETE | `null` (newIcon déjà null), puis `_whoAmI \|\| ''` en 7e position |
| ~7187 | `apiManageEntity` ADD Players | `_whoAmI \|\| ''` en 7e position du tableau |
| ~7200 | `apiManageEntity` ADD Categories | `_whoAmI \|\| ''` en 7e position du tableau |

> Note pour `apiManageEntity` : la signature GAS est `(action, type, newName, newMeta, oldName, newIcon, author)`. Le tableau callServer doit avoir 7 éléments. Exemple après la mise à jour :
> ```javascript
> callServer('apiManageEntity', ['DELETE', type, null, null, item.name, null, _whoAmI || ''], ...)
> callServer('apiManageEntity', ['ADD', 'Players', name, meta, null, null, _whoAmI || ''], ...)
> ```

- [ ] **Étape 3 : Mettre à jour les 11 call sites History**

| Ligne approx. | Fonction | Paramètre à ajouter |
|---|---|---|
| ~6012 | `apiAddBulkPlan` | `_whoAmI \|\| ''` après `payload` |
| ~6056 | `apiGroupRows` | `_whoAmI \|\| ''` après `indexes` |
| ~6153 | `apiDeleteHistoryEntries` (1re occur.) | `_whoAmI \|\| ''` après `allIndexes` |
| ~6196 | `apiDeleteHistoryEntries` (2e occur.) | `_whoAmI \|\| ''` après `rowIndexes` |
| ~6391 | `apiUngroupLot` | `_whoAmI \|\| ''` après `groupId` |
| ~6871 | `apiUpdateHistoryDescription` | `_whoAmI \|\| ''` après `v` |
| ~7010 | `apiUpdateBulkEntries` | `_whoAmI \|\| ''` après `fields` |
| ~7087 | `apiUpdateHistoryEntry` | `_whoAmI \|\| ''` après l'objet `{date, player, ...}` |
| ~7286 | `apiFixZeroPoints` | Remplacer `[]` par `[_whoAmI \|\| '']` |
| ~7297 | `apiDeleteOrphans` | Remplacer `[]` par `[_whoAmI \|\| '']` |
| ~7437 | `apiGroupDistributedLots` | `_whoAmI \|\| ''` après `lotsData` |

- [ ] **Étape 4 : Mettre à jour les 4 call sites Notes**

| Ligne approx. | Fonction | Paramètre à ajouter |
|---|---|---|
| ~6663 | `apiAddNote` (barre flash) | `_whoAmI \|\| ''` après `flashDate` |
| ~6742 | `apiAddNote` (bloc joueur) | `_whoAmI \|\| ''` après `npbDate` |
| ~6796 | `apiDeleteNote` | `_whoAmI \|\| ''` après `note.rowIndex` |
| ~6841 | `apiEditNote` | `_whoAmI \|\| ''` après `v` |

- [ ] **Étape 5 : Mettre à jour les 3 call sites Barème**

| Ligne approx. | Fonction | Paramètre à ajouter |
|---|---|---|
| ~7725 | `apiUpdateBaremeEntry` | `_whoAmI \|\| ''` après `ptsIn.value` |
| ~7743 | `apiDeleteBaremeEntry` | `_whoAmI \|\| ''` après `entry.rowIndex` |
| ~7768 | `apiAddBaremeEntry` | `_whoAmI \|\| ''` après `ptsIn.value` |

- [ ] **Étape 6 : Vérifier qu'aucun call site mutant n'a été oublié**

```bash
grep -n "callServer('apiAddBulkPlan\|callServer('apiDeleteHistoryEntries\|callServer('apiGroupRows\|callServer('apiUngroupLot\|callServer('apiUpdateHistoryDescription\|callServer('apiUpdateBulkEntries\|callServer('apiUpdateHistoryEntry\|callServer('apiFixZeroPoints\|callServer('apiDeleteOrphans\|callServer('apiGroupDistributedLots\|callServer('apiSetColor\|callServer('apiManageEntity\|callServer('apiAddNote\|callServer('apiDeleteNote\|callServer('apiEditNote\|callServer('apiAddBaremeEntry\|callServer('apiUpdateBaremeEntry\|callServer('apiDeleteBaremeEntry\|callServer('apiAddPhrase\|callServer('apiSavePhrasesBatch\|callServer('apiUpdatePhrase\|callServer('apiDeletePhrase\|callServer('apiDeletePreset\|callServer('apiRenamePreset" Index.html
```

Pour chaque ligne : vérifier que `_whoAmI || ''` est bien présent en dernier dans le tableau de params.

- [ ] **Étape 7 : Lancer tous les tests**

```bash
npm test
```

Expected: all tests PASS.

- [ ] **Étape 8 : Commit final**

```bash
git add Index.html
git commit -m "feat: pass _whoAmI to all mutating callServer calls — audit log fully wired"
```

---

## Récapitulatif des fichiers modifiés

| Fichier | Changements |
|---|---|
| `Code.gs` | +`AuditService` · `ConfigService` +`auditLog` · 7 helpers `_*Summary` · 26 `api*` +param `author` + `AuditService.log()` · +`apiGetAuditLog` |
| `Index.html` | CSS `history-*` + audit · HTML `#tab-history` → 2 sous-onglets · JS `initHistoryTabs` + `loadAuditLog` + `renderAuditTable` · ~35 `callServer` +`_whoAmI` |
| `tests/harness.js` | Exports +`AuditService` +`apiGetAuditLog` |
| `tests/audit.test.js` | Nouveau fichier — 9 tests |

---

## Self-review

**Couverture spec :**

| Exigence | Task |
|---|---|
| `AuditLog` auto-créée, 7 colonnes | 1 |
| `AuditService.log()` jamais throws | 1 (test silencieux) |
| `apiGetAuditLog` paginé et filtrable | 2 |
| Helpers before-state | 3 |
| 12 History mutations intégrées | 3 |
| 14 Settings/Notes/Barème/Phrases intégrées | 4 |
| CSS `history-*` scoped (pas de conflit) | 5 |
| `initHistoryTabs()` scopé à `#tab-history` | 6 |
| `loadAuditLog` + `renderAuditTable` | 6 |
| Avatar auteur dans le feed (règle UX) | 6 |
| Filtres + pagination | 6 |
| `_whoAmI` dans tous les callServer mutants | 7 |

**Placeholders :** aucun — tout le code est fourni dans chaque étape.

**Cohérence des signatures :**
- `AuditService.log(author, action, entity, before, after, detail)` — cohérent Tasks 1, 3, 4
- `apiGetAuditLog(page, pageSize, filterAuthor, filterAction, startDate, endDate)` — cohérent Tasks 2 et 6
- `apiManageEntity(action, type, newName, newMeta, oldName, newIcon, author)` — 7 paramètres, cohérent Tasks 4 et 7
