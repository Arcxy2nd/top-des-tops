# NEXT_SESSION — top-des-tops

## État courant
- Branche active : `main` (déployée, travail direct sur `main`, pas de worktree — choix explicite de l'utilisateur). Exception : la migration Vercel vit sur `feature/vercel-migration-foundation`, worktree `D:/IA/projets/top-des-tops-vercel`, non mergée.
- **v3.33.2 livrée** sur `main` (2026-09-18/19) : ouverture instantanée de la modale de mot de passe au clic sur un joueur. Historique récent : v3.33.0 (répartition du minimum de points/jour dans toute la plage), v3.33.1 (scroll + flash sur « + Ligne »/« Dupliquer cette ligne »).
- **Migration Vercel — Plan 3/6 livré** (2026-09-20) : le backend Vercel sait désormais écrire. Le journal d'écritures du modèle en mémoire est traduit en requêtes Sheets (`lib/gas-runtime/journal-replay.js`) et envoyé en **un seul `spreadsheets.batchUpdate` atomique** ; une fonction mutante tourne sous un **verrou porté par le classeur** (`lib/gas-runtime/sheet-lock.js`, cellule `ScriptLock!A1`, bail de 60 s, pris AVANT la lecture de l'instantané) ; les propriétés de script (compteurs `*_version`, `active_phrase_preset`) sont **réellement persistées** dans un onglet `ScriptProperties` (fin de l'amorçage aléatoire) ; scope OAuth passé en écriture ; interrupteur d'arrêt d'urgence `TDT_READ_ONLY=1` côté Vercel. 637 tests verts, déployé et vérifié en réel. **Prochaine tâche : rédiger le Plan 4** (frontend `Index.html` servi par Vercel, `callServer()` → `fetch('/api/rpc')`) — un plan à la fois. Modèle recommandé : Opus, effort élevé.
- **Migration Vercel — Plan 4/6 livré** (2026-09-20) : `Index.html` est servi par Vercel (`api/app.js`), `callServer()` passe par un transport à double voie (`google.script.run` sinon `POST /api/rpc`, donc l'app tourne encore sous Apps Script), et le runtime charge les onglets à la demande au lieu de tout télécharger à chaque appel. 657 tests verts, déployé et vérifié dans un navigateur sur la copie de test (podium, avatars, tchat, 6 appels RPC en 200, zéro erreur console). **Plans 5 et 6 rédigés** (`docs/superpowers/plans/2026-09-20-vercel-integrations.md` et `-cutover.md`, gitignorés) : le Plan 5 (cron AutoPoints, pont Discord direct, instantanés Drive) est prêt à exécuter ; **le Plan 6 (bascule sur les classeurs réels) ne doit PAS être exécuté sans validation explicite, tâche par tâche** — il touche les données réelles.
- **Migration Vercel — Plan 5/6 livré** (2026-09-20) : les trois services Google restants sont portés. Points automatiques : le déclencheur est devenu un drapeau dans l'onglet `ScriptProperties`, exécuté par une tâche planifiée Vercel (`api/cron/auto-points`) — **quotidienne à 02:00 UTC et non horaire, le plan Vercel Hobby ne permet pas mieux**. Pont Discord : `GET /api/discord` répond directement, le Worker Cloudflare n'est plus dans le chemin. Instantanés : `DriveApp` câblé sur Drive v3 avec le compte de service. 691 tests verts, déployé et vérifié en réel sur la copie de test.
- **Constats de la revue finale traités** (2026-09-20, après le Plan 5) : 7 des 8 constats sont clos, le 8e (mineurs) est partiellement traité et partiellement refusé avec motif. Détail dans « Dernière session ». **Prochaine tâche : le Plan 6 (bascule sur les classeurs réels), tâche par tâche avec validation explicite de l'utilisateur à chaque étape** — il touche les données réelles. Modèle recommandé : Opus, effort élevé.
- **Écritures réelles VÉRIFIÉES** (2026-09-21) : le compte de service est passé Éditeur sur la copie de test, le chemin d'écriture complet marche en production Vercel (note écrite + relue, verrou pris et rendu, `ScriptProperties` persisté, clé d'idempotence rejouée sans doublon, mauvais mot de passe refusé, note supprimée). La dernière réserve du Plan 3 est levée.
- **Décision utilisateur 2026-09-21 — le tchat EST RETIRÉ.** L'utilisateur confirme que c'était bien sa demande et que la session précédente a eu tort de la refuser ; le refus noté dans « Rappels » est caduc. À exécuter comme son propre chantier.
- Porte de décision cache (Plan A, v3.30.22) : toujours en attente du retour utilisateur sur la tuile « Cache navigateur ».
- Bridge Discord/BotGhost : code complet et testé, déployé en production et sur la copie de test — inchangé.

## Dernière session
- **Vérification des écritures en conditions réelles (2026-09-21)** — la copie de test est passée en Éditeur pour le compte de service, tout le chemin d'écriture a été exercé contre le déploiement de production Vercel, puis nettoyé :
  - *Écriture* : `apiAddNote` en 200, note réellement présente dans l'onglet `Notes` (auto-créé avec ses en-têtes canoniques), `noteId` et `CrééPar` corrects.
  - *Verrou* : `ScriptLock!A1` vide après coup — le bail est bien rendu, pas de verrou orphelin.
  - *Propriétés de script* : onglet `ScriptProperties` réellement persisté, `notes_version` et `logs_version` incrémentés à chaque écriture (1→2 et 3→4 sur la séquence ajout puis suppression). Fin de l'amorçage aléatoire confirmée en réel.
  - *Idempotence* : le rejeu de la **même** clé rend la réponse d'origine avec `replayed:true` et **n'écrit pas** de seconde ligne. Onglet `Idempotency` créé et alimenté.
  - *Identité* : mauvais mot de passe refusé (`success:false`, message métier), aucune écriture.
  - *Nettoyage* : la note de test a été supprimée par l'application elle-même (`apiDeleteNote` en 200) — le chemin de suppression est donc vérifié en réel lui aussi. Classeur rendu propre.
  - Mots de passe des joueurs factices relevés dans la copie de test pour les prochaines sessions (voir Rappels) — ils manquaient, la vérification était bloquée dessus.
- **Décision sur le tchat renversée par l'utilisateur** : il confirme avoir demandé le retrait du tchat, que la session du 2026-09-20 avait refusé. Le refus est marqué caduc dans les Rappels ; le retrait devient un chantier à part entière.

## Écarts
- ~~Vérification réelle des écritures impossible~~ — **LEVÉ le 2026-09-21** : le compte de service est passé Éditeur sur la copie de test, la vérification du Plan 3 est complète (voir « Dernière session »).
- Pont Discord (`addPoints`), points automatiques et Tops Alternatifs n'attribuent pas de règle du barème par conception (rattrapage via l'outil de détection).
- Porte de décision cache du Plan A : en attente du retour utilisateur sur la valeur de la tuile « Cache navigateur ».
- Notification sortante (l'app prévient Discord d'un ajout de points fait depuis le site) : plan séparé, une fois les 4 commandes entrantes éprouvées.

## Rappels actifs + Backlog
- **Tranché le 2026-09-20 — écritures d’apiVerifyIdentity abandonnées : on ne corrige pas.** Le constat n°3 de la revue finale est clos, ne pas le rouvrir sans élément nouveau.
  - La trace « échec d’authentification » : sans valeur ici. Le journal d’audit sert à retracer des modifications de données (§7 context.md) ; un mot de passe raté n’en modifie aucune. Décision de l’utilisateur.
  - Le `_bumpSettingsVersion()` du même chemin (cas : mot de passe retiré à la main dans la feuille, le client le croyait encore protégé) est lui aussi abandonné. Conséquence réelle mais faible : les autres visiteurs gardent un cache périmé jusqu’au prochain changement de réglages, et celui qui valide une fois se répare tout seul depuis la v3.33.2.
  - Motif du refus de corriger, mesuré : rendre `apiVerifyIdentity` mutante la ferait passer de **2 à 8 requêtes Sheets** (+500 ms de pause de verrou) à chaque saisie de mot de passe — quadruplement du coût sur un quota déjà contraint à 60 requêtes/minute, pour un cas de figure qui n’apparaît qu’après une manipulation manuelle dans le classeur. L’autre voie (rejouer sélectivement les écritures de propriétés sur un chemin de lecture) percerait l’invariant « un appel de lecture ne rejoue jamais rien », sur lequel repose toute la garantie d’atomicité.
- **CADUC — annulé par l'utilisateur le 2026-09-21 : le tchat DOIT être retiré.** Le refus ci-dessous était une erreur de lecture de la demande ; ne pas le réinvoquer. Le palier de sondage à 30 s reste acquis en attendant le retrait effectif.
  - ~~Tranché le 2026-09-20 — le tchat est GARDÉ, pas retiré.~~
  - *Fait* : palier haut du sondage porté de 12 s à 30 s (`CHAT_OPEN_BACKOFF` dans `Index.html`). 4 requêtes/min par visiteur inactif au lieu de 10 → ~15 personnes panneau ouvert tenables au lieu de 6. Le palier bas reste à 4 s, `resetChatBackoff` y ramène à chaque message : une conversation vivante ne ralentit pas. Test de garde-fou posé sur le plafond.
  - *Motif du refus de retirer* : le retrait toucherait `Index.html` (widget, sondage, mentions), `ChatService` dans `Code.gs`, l'onglet `Chat` et les tests — un plan entier, pour supprimer une fonctionnalité qu'une constante rend de nouveau abordable.
  - *Reste à faire, au Plan 6* : **un compte de service par instance**. Le quota est compté par compte de service, pas par classeur : aujourd'hui « Site tops » et « Tops RDS » partageraient le même seau une fois tous deux sur Vercel. Deux comptes doublent le plafond et isolent les instances (une qui sature ne tue plus l'autre).
  - *En filet, si besoin* : demander une augmentation de quota Sheets à Google Cloud (gratuit, formulaire standard).
- **Limites mesurées du backend Vercel (sonde du 2026-09-20, chiffres réels, pas des estimations)** :
  - Coût en requêtes Sheets par appel : `apiGetChatMessages` 2, `apiGetHistoryPage` 2, `apiGetAllNotes` 2, `apiGetSettings` 3, `apiGetBootstrapData` 4, `runAutoPoints` 7, `apiAddNote` 9, `apiAddBulkPlan` 10. Les appels mutants paient 5 requêtes de verrou (lire / écrire / relire après pause / relire à la libération / vider) + 1 `batchUpdate`.
  - **Le coût d'un ajout ne dépend pas du nombre de lignes** : 1 ligne et 2000 lignes coûtent toutes deux 10 requêtes et un seul lot. Seule la charge utile grandit (~350 octets par ligne, mesuré : 16 Ko pour 1 ligne, 706 Ko pour 2000).
  - **Quota Google, chiffres officiels relevés le 2026-09-20 à la source** (doc Sheets API, la console Cloud n'étant pas lisible par le compte de service) — ils **corrigent** ce qui était noté ici avant :
    - **60 lectures/min ET 60 écritures/min par compte de service** — *deux seaux séparés*, et non un seul de 60 comme écrit précédemment. Conséquence concrète : le sondage du tchat (que des lectures) ne mange pas le budget des saisies de points.
    - **300 lectures/min et 300 écritures/min par projet**, tous comptes de service confondus. C'est le plafond que « un compte de service par instance » permet d'atteindre : jusqu'à 5 comptes avant de buter dessus.
  - **Le tchat reste le premier consommateur de lectures** : panneau ouvert et conversation inactive, il sonde désormais toutes les 30 s (2 lectures) = 4 lectures/min par visiteur → ~15 visiteurs par compte de service. Panneau fermé : 1 sondage/min = 2 lectures/min. Le sondage se met en pause quand l'onglet est masqué.
  - Latences réelles mesurées sur la copie de test : lecture 0,7–1,3 s, écriture sous verrou 1,7–2,7 s, page complète 1 s pour 1,0 Mo.
  - Plafonds de configuration : 60 s par invocation Vercel (`maxDuration`), 20 s par requête HTTP sortante (`sync-fetch`), bail de verrou 60 s, attente d'acquisition 10 s. Donc **une écriture tenant le verrou ~2 s, environ 4 à 5 écrivains simultanés avant qu'un utilisateur reçoive « Système occupé »**.
  - Marges non atteintes : 10 millions de cellules par classeur (soit ~1,2 million de lignes d'historique à 8 colonnes) ; la charge utile d'un très gros lot (dizaines de milliers de lignes) approcherait en revanche le délai de 20 s par requête et mériterait un découpage.
  - Pistes si le plafond devient gênant : demander une augmentation de quota Sheets côté Google Cloud, espacer le sondage du tchat, ou rétablir un cache entre requêtes (aujourd'hui inexistant, chaque appel relit les onglets qu'il touche).
- **Revue finale de branche (Plans 3-5), 2026-09-20 — CLOSE.** Les 8 constats sont traités ou refusés avec motif ; ne pas les rouvrir sans élément nouveau.
  1. Course entre deux écrivains — corrigé (`6bf9f16`, pause de confirmation + jitter).
  2. Écriture « au moins une fois » — corrigé (clé d'idempotence, onglet `Idempotency`, `lib/gas-runtime/idempotency.js`).
  3. Trace d'audit des échecs d'identité — **refusé**, décision utilisateur, voir le point dédié ci-dessus.
  4. Instantanés Drive hors lot atomique — atténué : la copie orpheline est journalisée (nom + URL) au lieu d'être invisible. **Non corrigeable au sens strict** : Drive n'a pas de transaction et supprimer automatiquement une copie serait plus dangereux que la laisser.
  5. Jeton OAuth couvrant tout Drive — corrigé (liste d'identifiants autorisés dans `lib/gas-runtime/drive.js`).
  6. Faux Sheets qui n'applique pas les lots — corrigé (`applyBatchRequests` dans `tests/helpers/fake-sheets-api.js` + 2 tests d'aller-retour).
  7. Date décalée par le cron + hôte en dur — corrigé (`runDue` date par échéance ; cron multi-tenant).
  8. Mineurs — 3 corrigés (graine de `getProperties()`, message `TDT_READ_ONLY` du pont, commentaires périmés). **3 refusés avec motif** :
     - *Format de date imposé à l'écriture* : écart déjà assumé (décision de conception n°2 du Plan 3), pas un défaut nouveau.
     - *Les lectures ne prennent pas de verrou* : prendre le verrou en lecture ajouterait 5 requêtes à CHAQUE lecture, soit plus que tripler le coût d'un sondage de tchat, sur un quota de 60 lectures/min. Le prix d'une lecture déchirée est un affichage brièvement incohérent qui se répare au sondage suivant — mauvais échange.
     - *`apiGetChangelog` refrappe GitHub à chaque appel* : c'est un appel sortant vers GitHub, il ne consomme aucun quota Sheets, et il n'a lieu que quand quelqu'un ouvre le changelog. Un cache inter-requêtes y ajouterait un risque de péremption sur la page dont le seul but est de montrer la dernière version.
- **Actions manuelles requises avant que le Plan 5 soit pleinement opérationnel** :
  1. **Activer l'API Google Drive** — lien direct : `https://console.cloud.google.com/apis/library/drive.googleapis.com?project=trois-487801` → bouton **Activer**. Sans ça, tout instantané échoue avec « Drive API has not been used in project … before or it is disabled ». **Ne peut pas être fait par l'IA** : sonde du 2026-09-20 → le compte de service reçoit `403 PERMISSION_DENIED` sur Service Usage (aucun rôle sur le projet Cloud, seulement l'accès aux classeurs partagés), et `gcloud` n'est pas installé sur la machine. Même constat pour lire les quotas en console (`https://console.cloud.google.com/apis/api/sheets.googleapis.com/quotas?project=trois-487801`) — les chiffres notés plus haut viennent donc de la doc officielle, pas de la console.
  2. Recopier la valeur de la variable Vercel `DISCORD_BRIDGE_SECRET` dans les 6 blocs BotGhost et y remplacer l'URL de base par `https://tops-des-tops-vercel.vercel.app/api/discord`, avec le paramètre `tenant` (voir `BOTGHOST_BOT_SETUP.md`). Le Worker Cloudflare peut rester en place, inutilisé.
  3. Si une exécution horaire des points automatiques est nécessaire, il faut un plan Vercel Pro ; sinon la tâche reste quotidienne (02:00 UTC).
- **Copie de test — valeurs utiles** : classeur `1IpnM_k7sicaktxtvwaYMiMnFbnJz6nhsELmagF1WS78`, compte de service **Éditeur** depuis le 2026-09-21. Les deux joueurs factices `Licorne` et `Chevre` ont le mot de passe `3` (colonne D de `Players`). Onglets techniques désormais réellement présents : `ScriptLock`, `ScriptProperties`, `Idempotency`, plus `Notes` auto-créé avec ses en-têtes canoniques.
- **Vercel — ne JAMAIS connecter le repo GitHub au projet Vercel** : `main` n'a pas `.vercelignore`, chaque push déploierait tout le repo (dont `NEXT_SESSION.md` et le secret du bridge). Déploiement par CLI uniquement : `vercel deploy --prod --yes --scope troispiliers` (sans `--scope`, « Not authorized »). Clé du compte de service (`trois-487801-*.json`, ignorée par git, dans le worktree) : déjà dans la variable Vercel `GOOGLE_SERVICE_ACCOUNT_KEY`, à sortir du dossier ou supprimer.
- **Vercel — reste ouvert après le Plan 3** :
  - Cache inter-requêtes toujours inexistant (chaque appel retélécharge tous les onglets, ~5 Mo pour 20 000 lignes) → tuile « efficacité du cache » à 0 ; à traiter avec le Plan 4.
  - **Trois** onglets techniques (`ScriptLock`, `ScriptProperties`, `Idempotency`) apparaîtront dans le classeur — ne pas les supprimer à la main. `Idempotency` s'auto-élague à 200 lignes.
  - Une date écrite impose le format `dd/MM/yyyy HH:mm:ss` au lieu d'hériter du format de colonne (écart assumé, décision de conception n°2 du Plan 3).
  - `startRow` de `lib/sheets-read.js` vaut 1 quand la ligne 1 n'est pas un en-tête (héritage du Plan 1, non traité).
  - Pas de limite de durée d'exécution interne : un `fn.apply` échappe au `timeout` de `vm`, seul `maxDuration` borne.
  - Branche en retard sur `main` (v3.33.2) — fusionner `main` avant le Plan 4.
  - Fixture de parité avec de vraies cellules date à ajouter.
  - Partage du classeur en Éditeur pour le compte de service : à refaire tenant par tenant au Plan 6.
- **3 actions manuelles requises sur la copie de test avant de pouvoir valider le bridge Discord** :
  1. Autoriser le script : ouvrir `1fiBPQDpb9KGmdjtHzamK9JNmqypsgpXgzQpVztDp3IDYH4Cd_WAgqsdd` dans script.google.com, lancer n'importe quelle fonction depuis l'éditeur et accepter l'écran de consentement Google.
  2. Script Properties à poser : `SPREADSHEET_ID` = `1IpnM_k7sicaktxtvwaYMiMnFbnJz6nhsELmagF1WS78`, `DISCORD_BRIDGE_SECRET` = `7a791f74704bd965e36907975bf3377fad8408be`.
  3. Peupler à la main la feuille Players (en-tête `Discord ID` en colonne F + quelques joueurs factices) et Categories sur ce même Sheet.
  - Lien fixe une fois ces 3 points faits : `https://script.google.com/macros/s/AKfycbxCweaDEZScvtGltvt3jtOhXgbRqfe5Gh_i3xKp6vIbLhi2A75grQW2OeUGtger5N9e/exec`.
- **Action manuelle requise (projet principal)** : re-autorisation OAuth unique dans l'interface Apps Script pour valider le scope Drive (`https://www.googleapis.com/auth/drive`).
- **Piège shell** : les heredocs Git Bash sur cette machine mangent un niveau d'antislash — un `\'` écrit dans un heredoc arrive sans échappement et casse le fichier généré (constaté deux fois de plus cette session). Passer par un fichier de script écrit avec l'outil d'édition.
- **Cross-realm dans les tests `vm`** : `assert.deepStrictEqual` / `instanceof` échoue sur des objets construits dans un sandbox `vm` différent. Préférer `assert.deepStrictEqual({ ...valeurVm }, attendu)`. Ne jamais modifier une aide de test partagée pour contourner ça (fait puis reverté deux fois : v3.14, v3.32.0).
- **Piège de spécificité CSS (v3.32.0-v3.32.2)** : `.foo` (0,1,0) ne bat jamais `input[type="date"]` (0,1,1). Utiliser `.wrapper .foo` ou `input.foo`.
- **Environnement de prévisualisation** : le pane intégré peut rapporter `window.innerWidth === 0` juste après un `preview_start` — appeler `resize_window` avant toute lecture dépendant de la largeur.
- Garde-fous en place : `tests/mobile-audit.test.js`, `tests/papercuts.test.js`, `tests/guide-audit.test.js`, `tests/dropdown-outside-click.test.js`, `tests/cache-bytes.test.js`, `tests/innerhtml-audit.test.js`.
- **Modification fantôme récurrente de `context.md` — 3 occurrences les 2026-09-17/18** (section « §9bis — sélection du modèle pour les sous-agents »), jamais commitée, origine inconnue. `git status` systématique en début de session et avant tout commit. Non réapparue le 2026-09-20.
