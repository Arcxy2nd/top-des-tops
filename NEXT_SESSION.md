# NEXT_SESSION — top-des-tops

## État courant
- Branche active : `main` (déployée, travail direct sur `main`). La branche `feature/vercel-migration-foundation` est **fusionnée** (`7a71da3`) ; son worktree `D:/IA/projets/top-des-tops-vercel` ne sert plus qu'à garder la clé du compte de service (`trois-487801-*.json`, ignorée par git).
- **v3.35.0 — BASCULE VERCEL FAITE (2026-09-21, 04:15 UTC).** Les deux instances sont servies par Vercel depuis leurs vrais classeurs : `c55zvj.s.gy/tops-des-tops` → `tops-site-tops.vercel.app`, `c55zvj.s.gy/top-RDS` → `tops-rds.vercel.app`. Écritures ouvertes (`TDT_READ_ONLY` retiré). Déploiement désormais **par CLI depuis `main`** : `vercel deploy --prod --yes --scope troispiliers` (le dossier `main` est relié au projet Vercel). Détails, tenants, variables : `context.md` §10.
- **v3.34.0 — tchat retiré** (même jour), livré avec la bascule.
- **Retour arrière** (si problème) : `gh workflow run repoint-shortlinks.yml -f site_tops_url=https://script.google.com/macros/s/AKfycbyNcMjVqaYLqvd86gNUiKEkP3XRqhIEQd1r7YkJJmDCUUdj1l2L5rJvDg7y9vzZEka4/exec -f tops_rds_url=https://script.google.com/macros/s/AKfycbwStoRlnOrPlNNAwqtK-iI__N2CDyj0rdi969t_V4gAdVFYM5lrz7tBeyBTBdO511ZDZg/exec` — les déploiements Apps Script sont intacts (mais servent l'ancienne version, avec tchat).
- **v3.38.0 LIVRÉE (2026-09-25) — refonte de l'affichage des avatars** : filigranes d'avatar repensés en arrière-plan des cartes de notes (`.note-card-bg`) avec masque dégradé progressif et dynamisme au survol, anneaux colorés universels aux couleurs du joueur (`--player-color`) sur les colonnes de notes, footers auteurs, mentions inline, Quick Stats et Qui suis-je ; détection des mentions entre parenthèses `(@Joueur)` et masque dégradé sur le bandeau flash. 773/773 tests passés.
- **v3.37.0 LIVRÉE (2026-09-25) — optimisation quota Sheets**, plan `docs/superpowers/plans/2026-09-25-sheets-quota-optimization.md` exécuté 5/5 (768/768 tests). Mesuré sur harness : lecture 3-4 → **1** (0 si cache chaud), écriture 6R/3W → 3R/3W (verrou Sheets) ou **0R/1W** (Upstash). **En attente utilisateur** : (1) activer l'API Drive (`https://console.cloud.google.com/apis/library/drive.googleapis.com?project=trois-487801`) — sans elle le cache est inactif, repli 1 lecture/RPC ; (2) Upstash via Vercel Marketplace (optionnel). **Prochaine tâche** : après activation Drive, mesurer le délai de changement de version Drive sur la copie de test (étape 9 phase 3, script hors dépôt) et vérifier `[quota] ... sheetsReads=0` dans `vercel logs` au 2ᵉ F5 sur `tops-des-tops-vercel.vercel.app`.
- **Ancienne tâche, TRAITÉE par v3.37.0 (2026-09-25) : optimiser le site pour tenir dans 60 lectures Sheets/min** (compte de service partagé par les 2 instances). Objectif vérifiable : un utilisateur seul peut rafraîchir et éditer sans jamais voir de 429. Démarche : `/superpowers:systematic-debugging` → **mesurer d'abord** (compteur de requêtes Sheets par RPC et par rafraîchissement, sur la copie de test ou le harness), puis plan. Cible : rafraîchir sans changement = **0** lecture Sheets ; après un changement = 1 lecture partagée par tous les visiteurs ; écriture = ≤ 1 lecture + 1 écriture. Leviers (brainstorm 2026-09-25, **aucun mesuré**) :
  - **Serveur** : (a) métadonnées + onglets en 1 seul appel, et lire tout le classeur d'un coup (≈1 Mo) → 1 lecture max par RPC ; (b) cache mémoire du classeur dans l'instance Vercel chaude, mis à jour en place par nos propres écritures (`applyBatchRequests` des tests à promouvoir en `lib/`) ; (c) **Upstash Redis gratuit** comme cache partagé entre instances + verrou (`SET NX PX`) + idempotence + compteurs de version → le verrou `ScriptLock` (3 lectures + 2 écritures par modif) disparaît ; (d) la réponse d'une écriture renvoie les données à jour → plus de rechargement derrière ; (e) réessai avec attente sur 429 + file d'attente au lieu d'un échec ; (f) lectures servies en GET avec cache CDN Vercel indexé par version.
  - **Fraîcheur hors quota Sheets** : les modifs manuelles du classeur (mots de passe, Discord ID) et l'ancien déclencheur GAS ne changent pas nos versions → contrôle via Drive API `files.get?fields=version` (quota Drive séparé, bien plus large ; API Drive à activer) ou durée de vie max du cache. Piste à mesurer : lire tout le classeur par export Drive (xlsx) au lieu de l'API Sheets.
  - **Client** : bootstrap vraiment unique (stats rapides, 1ʳᵉ page d'historique, barème dedans) ; fusion des appels identiques en vol ; pagination, recherche et filtres de dates de l'historique en local (≈400 lignes) ; rien pendant qu'un onglet est caché ; BroadcastChannel entre onglets du même navigateur ; validation « version inchangée → rien à relire ».
  - **Gratuit, sans code** : demande de hausse de quota Sheets ; un compte de service par instance (budget doublé).
  - **Garde-fou** : compteur de requêtes Sheets par RPC dans les logs + tests de budget (ex. bootstrap ≤ 1 lecture).
  - **Refusé** : envoi différé des points toutes les 3 min (idée utilisateur) → interface optimiste interdite par `context.md`, perte si l'onglet se ferme, et les écritures ont leur propre seau : ce qui coûte des lectures à une écriture, c'est le verrou et le rechargement, traités par (c) et (d). Le regroupement existe déjà : un lot de 2000 lignes coûte autant qu'une ligne.
- **Migration vers une base de données (Neon + copie Drive en consultation) : EN PAUSE** — brainstormée le 2026-09-25 (choix : base = source de vérité, copie Drive consultation seule, modifications par l'app seulement, gratuit, `Code.gs` inchangé), puis mise de côté au profit de l'optimisation. Trou identifié à régler si elle reprend : Discord ID (col. F) et mots de passe (col. D) de `Players` n'ont **aucun écran dans l'app**, ils ne se saisissent qu'à la main dans le classeur.
- **Ensuite** : basculer les points automatiques d'Apps Script vers Vercel (l'utilisateur supprime le déclencheur dans l'éditeur Apps Script des 2 projets, puis réactivation depuis l'app, Paramètres → Automatisations, sur chaque instance). En parallèle, hors agent : le développeur du bot configure BotGhost sur la **copie de test** (guide `docs/guide-bot-discord-vercel.pdf`) puis les vrais sites ; l'utilisateur remplit la colonne F « Discord ID » des vrais classeurs. Archivage des déploiements Web App Apps Script : seulement après la période d'observation (supprime le retour arrière). Init recommandé : light.
- Porte de décision cache (Plan A, v3.30.22) : toujours en attente du retour utilisateur sur la tuile « Cache navigateur ».
- **v3.36.0 livrée (2026-09-23)** : `docs/superpowers/plans/2026-09-23-alt-tops-management-parity.md` exécuté intégralement (4/4 tâches). Tops Alternatifs gérés comme les Tops (édition nom/description/emoji/couleur, ordre ▲/▼, suppression par ligne, journal + annulation) via `SettingsService` type `AltCategories`. `npm run verify` 727/727.

## Dernière session
- **2026-09-25 — refonte de l'affichage des avatars (v3.38.0)** :
  - `Index.html` : refonte du filigrane d'arrière-plan `.note-card-bg` sur chaque carte de note (masque dégradé progressif `linear-gradient(to left, black 25%, rgba(0,0,0,0.6) 60%, transparent 95%)`, opacité discrète 0.14 avec fondu hover 0.25 / scale 1.04, isolation de profondeur `z-index` pour garantir une parfaite lisibilité du texte, blend mode sombre en thème clair) ; ajout de l'anneau coloré (`--player-color`) sur les avatars de colonne (`.npb-head img`) et liseré supérieur sur `.note-player-block` ; anneau coloré sur les avatars auteurs (`.note-meta-avatar`) et épuration du texte (« Créé par », « Modifié par » sans emoji ✍️/📝 redondant) ; micro-anneau et centrage vertical sur `.mention img` avec détection élargie aux parenthèses `(@Joueur)` et crochets `[@Joueur]` ; anneau coloré sur `.qs-avatar` (Leader, Dernier Event, Record) et `.who-am-i-avatar` ; masque dégradé sur `.notes-flash-avatar-bg`.
  - Tests : création de `tests/avatar-redesign.test.js` (5 tests verts vérifiant la présence et la mise en page des filigranes repensés, les masques et les anneaux). 773/773 tests passés à 100%.
- **2026-09-25 — optimisation quota Sheets (v3.37.0)** : plan exécuté 5/5.
- **2026-09-23 — exécution du plan Tops Alternatifs (v3.36.0) + un correctif hors-plan (v3.35.1)**, en sous-agents (implémenteur + relecteur par tâche) :
  - **v3.35.1 (hors-plan, signalé en direct par l'utilisateur pendant l'exécution)** : le menu déroulant « Saiseur » (et tout menu du même composant) s'affichait derrière une modale ouverte — `.rs-panel` (reparenté sous `<body>` à l'ouverture) avait un z-index (9999) inférieur à `.modal-backdrop` (10500) dont il devient alors le frère, pas le descendant. Corrigé (`d1340a8`, z-index → 10600), garde-fou ajouté (`tests/mobile-audit.test.js`), poussé et déployé immédiatement.
  - **Tâche 1** (`Code.gs`) : `SettingsService` accepte le type `AltCategories` (feuille dédiée, colonne `Ordre`, cascade de renommage vers `AltHistory` colonne C, protection homonymes). Relecture : 1 tour de fix (régression de test collatérale sur `tests/headerless-sheets.test.js`, corrigée).
  - **Tâche 2** (`Code.gs`) : `apiManageEntity` / `apiSetColor` / `apiReorderEntities` prennent en charge `AltCategories` avec journal d'audit et annulation ; `apiSaveAltCategories` (réécriture complète de la feuille à chaque action) supprimée. Relecture propre au premier passage.
  - **Tâche 3** (`Index.html`) : liste des Tops Alternatifs alignée sur `renderEntityList`/`openEditModal` des Tops — édition, couleur (palette par défaut si non choisie, plus de couleur aléatoire), ▲/▼, suppression, bouton « Gérer les points », interface optimiste supprimée sur les couleurs (Joueurs compris, corrigé en miroir). Relecture propre au premier passage. Vérification manuelle faite par le contrôleur (pas par sous-agent) sur le harness local : ajout/renommage/couleur/réordonnancement/suppression/annulation Journal, thème clair, mobile 375px — tout correct.
  - **Tâche 4** : guide inline, `CHANGELOG.md` (v3.36.0), `context.md` §3/§4, revue de code deux axes (Standards : 1 point déjà résolu + 2 pistes de refactor mineures non bloquantes ; Spec : aucun écart).
  - Détail de la session de bascule du 2026-09-21 et de la relecture du 2026-09-22/23 : historique git de ce fichier et `CHANGELOG.md`.

## Écarts
- **Pont Discord : configuration BotGhost confiée au développeur du bot** (2026-09-21), qui teste sur la copie de test. Guide PDF : `docs/guide-bot-discord-vercel.pdf`. **Il n'y a jamais eu de relais Cloudflare** (correction de l'utilisateur, 2026-09-21) : il était documenté mais jamais mis en place — le bot n'a donc jamais pu appeler Apps Script (redirection 302) et n'a jamais fonctionné en production. Toutes les mentions ont été retirées. **Secret du pont régénéré** — en clair dans `BOT_SECRET.txt`, versionné à la demande explicite de l'utilisateur. Vérifié : copie de test = toutes commandes OK, écriture comprise (`addNote` via le pont, puis supprimée) ; vrais sites = `getLeaderboard`/`listTops`/`listBareme` OK. **Bloquant pour les écritures sur les vrais sites** : aucun `Discord ID` dans `Players` (colonne F absente) — le dev collecte les ID, l'utilisateur remplit la colonne.
- **Points automatiques toujours exécutés par le déclencheur horaire Apps Script — à basculer (prochaine tâche)** : le cron Vercel les ignore car `auto_trigger_installed` n'est pas posé côté Vercel. Risque constaté le 2026-09-22 : l'ancien code Apps Script écrit dans les vrais classeurs sous son propre verrou Google, invisible pour le verrou `ScriptLock` de Vercel → une exécution automatique peut croiser une saisie. Probable aussi (non vérifié) : ses compteurs `*_version` vont dans les propriétés GAS, pas dans l'onglet `ScriptProperties` → caches navigateur non invalidés. Pour les passer sur Vercel : supprimer le déclencheur GAS (éditeur Apps Script → Déclencheurs) **puis** réinstaller depuis l'app (Paramètres → Automatisations). Jamais les deux en même temps. Note : le cron Vercel est quotidien (02:00 UTC), pas horaire.
- **API Drive désactivée** sur le projet Cloud `trois-487801` → les instantanés lancés depuis l'app sur Vercel échouent. **Non traité volontairement** : l'utilisateur n'utilise pas les instantanés. Si besoin un jour : `https://console.cloud.google.com/apis/library/drive.googleapis.com?project=trois-487801` → Activer.
- **Task 5 du Plan 6 FAITE côté dépôt** (2026-09-21, sur demande explicite de l'utilisateur) : workflow GAS, `clasp`, `strip-comments.js`, `deploy-targets.json`, `.claspignore`, `SETUP-AUTOSYNC.md` supprimés ; `DEPLOIEMENT.md` réécrit ; `push.bat` déploie sur Vercel. **Reste côté Google (manuel, Step 5)** : archiver les déploiements Web App GAS (plus rien n'en dépend : le bot n'a jamais utilisé Apps Script). Retour arrière toujours possible tant que ces déploiements existent.
- Historique : nombre d'entrées (415 / 208) et de notes (14 / 32) relevés côté Vercel seulement, non recoupés visuellement côté GAS.
- **Écart mineur, justifié (plan v3.36.0)** : le plan attendait `grep -n "ALT_FALLBACK_COLOR" Index.html` → 4 lignes ; il y en a 5. La 5ᵉ (`Index.html` ~l.9991) est un badge d'état statique (« ce Top Alt est le filtre actif ») qui utilise cette constante comme couleur d'accent fixe, pas comme couleur d'entité — la convertir en `altCategoryColor()` changerait son sens. Non corrigé, volontairement.
- Pont Discord (`addPoints`), points automatiques et Tops Alternatifs n'attribuent pas de règle du barème par conception.
- Notification sortante vers Discord : plan séparé.
- **Erreur 429 vue en production (2026-09-23, rapportée par l'utilisateur)** : « Chargement historique : Erreur de connexion BDD : Échec Sheets API (429) : lecture des métadonnées » — quota de lectures Sheets dépassé. **Reproduit le 2026-09-25 par l'utilisateur SEUL** : 1 modification d'historique + 3 rafraîchissements = plafond atteint (logs Vercel 10:47-10:48 : ~20 RPC/min, dont 5 RPC simultanés par rafraîchissement ; 429 sur `apiUpdateHistoryEntry` et `apiGetQuickStats`). **Verdict utilisateur : manque d'optimisation sérieux, pas une fatalité du quota** → voir « Prochaine tâche ». Les autres messages console collés (CSP d'une extension, favicon 404, meta `apple-mobile-web-app-capable` dépréciée) sont sans lien.

## Rappels actifs + Backlog
- **Tranché le 2026-09-20 — écritures d’apiVerifyIdentity abandonnées : on ne corrige pas.** Le constat n°3 de la revue finale est clos, ne pas le rouvrir sans élément nouveau.
  - La trace « échec d’authentification » : sans valeur ici. Le journal d’audit sert à retracer des modifications de données (§7 context.md) ; un mot de passe raté n’en modifie aucune. Décision de l’utilisateur.
  - Le `_bumpSettingsVersion()` du même chemin (cas : mot de passe retiré à la main dans la feuille, le client le croyait encore protégé) est lui aussi abandonné. Conséquence réelle mais faible : les autres visiteurs gardent un cache périmé jusqu’au prochain changement de réglages, et celui qui valide une fois se répare tout seul depuis la v3.33.2.
  - Motif du refus de corriger, mesuré : rendre `apiVerifyIdentity` mutante la ferait passer de **2 à 8 requêtes Sheets** (+500 ms de pause de verrou) à chaque saisie de mot de passe — quadruplement du coût sur un quota déjà contraint à 60 requêtes/minute, pour un cas de figure qui n’apparaît qu’après une manipulation manuelle dans le classeur. L’autre voie (rejouer sélectivement les écritures de propriétés sur un chemin de lecture) percerait l’invariant « un appel de lecture ne rejoue jamais rien », sur lequel repose toute la garantie d’atomicité.
- **Tchat retiré en v3.34.0 — ne pas le réintroduire.** Règle figée dans `context.md` (« RÈGLE — PAS DE TCHAT DANS L'APPLICATION ») et garde-fou `tests/chat-removed.test.js`. L'onglet `Chat` des classeurs reste en place, simplement plus lu ni écrit. Effet de bord favorable pour le Plan 6 : le tchat était le premier consommateur de lectures Sheets (4 lectures/min par visiteur panneau ouvert) — ce poste disparaît entièrement du budget de quota.
- **Limites mesurées du backend Vercel (sonde du 2026-09-20, chiffres réels, pas des estimations)** :
  - Coût en requêtes Sheets par appel : `apiGetHistoryPage` 2, `apiGetAllNotes` 2, `apiGetSettings` 3, `apiGetBootstrapData` 4, `runAutoPoints` 7, `apiAddNote` 9, `apiAddBulkPlan` 10. Les appels mutants paient 5 requêtes de verrou (lire / écrire / relire après pause / relire à la libération / vider) + 1 `batchUpdate`. (Mesure du 2026-09-20 ; l'entrée `apiGetChatMessages` a disparu avec le tchat en v3.34.0.)
  - **Le coût d'un ajout ne dépend pas du nombre de lignes** : 1 ligne et 2000 lignes coûtent toutes deux 10 requêtes et un seul lot. Seule la charge utile grandit (~350 octets par ligne, mesuré : 16 Ko pour 1 ligne, 706 Ko pour 2000).
  - **Quota Google, chiffres officiels relevés le 2026-09-20 à la source** (doc Sheets API, la console Cloud n'étant pas lisible par le compte de service) — ils **corrigent** ce qui était noté ici avant :
    - **60 lectures/min ET 60 écritures/min par compte de service** — *deux seaux séparés*, et non un seul de 60 comme écrit précédemment. Les lectures et les saisies de points ne se disputent donc pas le même budget.
    - **300 lectures/min et 300 écritures/min par projet**, tous comptes de service confondus. C'est le plafond que « un compte de service par instance » permet d'atteindre : jusqu'à 5 comptes avant de buter dessus.
  - **Le premier consommateur de lectures a disparu avec le tchat (v3.34.0)** : il coûtait 4 lectures/min par visiteur panneau ouvert, soit ~15 visiteurs par compte de service à lui seul. Depuis son retrait, une page ouverte ne consomme plus que ses chargements ponctuels — le plafond de visiteurs simultanés n'est plus contraint par un sondage de fond.
  - Latences réelles mesurées sur la copie de test : lecture 0,7–1,3 s, écriture sous verrou 1,7–2,7 s, page complète 1 s pour 1,0 Mo.
  - Plafonds de configuration : 60 s par invocation Vercel (`maxDuration`), 20 s par requête HTTP sortante (`sync-fetch`), bail de verrou 60 s, attente d'acquisition 10 s. Donc **une écriture tenant le verrou ~2 s, environ 4 à 5 écrivains simultanés avant qu'un utilisateur reçoive « Système occupé »**.
  - Marges non atteintes : 10 millions de cellules par classeur (soit ~1,2 million de lignes d'historique à 8 colonnes) ; la charge utile d'un très gros lot (dizaines de milliers de lignes) approcherait en revanche le délai de 20 s par requête et mériterait un découpage.
  - Pistes si le plafond devient gênant : demander une augmentation de quota Sheets côté Google Cloud, ou rétablir un cache entre requêtes (aujourd'hui inexistant, chaque appel relit les onglets qu'il touche).
- **Revue finale de branche (Plans 3-5), 2026-09-20 — CLOSE.** Les 8 constats sont traités ou refusés avec motif ; ne pas les rouvrir sans élément nouveau.
  1. Course entre deux écrivains — corrigé (`6bf9f16`, pause de confirmation + jitter).
  2. Écriture « au moins une fois » — corrigé (clé d'idempotence, onglet `Idempotency`, `lib/gas-runtime/idempotency.js`).
  3. Trace d'audit des échecs d'identité — **refusé**, décision utilisateur, voir le point dédié ci-dessus.
  4. Instantanés Drive hors lot atomique — atténué : la copie orpheline est journalisée (nom + URL) au lieu d'être invisible. **Non corrigeable au sens strict** : Drive n'a pas de transaction et supprimer automatiquement une copie serait plus dangereux que la laisser.
  5. Jeton OAuth couvrant tout Drive — corrigé (liste d'identifiants autorisés dans `lib/gas-runtime/drive.js`).
  6. Faux Sheets qui n'applique pas les lots — corrigé (`applyBatchRequests` dans `tests/helpers/fake-sheets-api.js` + 2 tests d'aller-retour).
  7. Date décalée par le cron + hôte en dur — corrigé (`runDue` date par échéance ; cron multi-tenant).
  8. Mineurs — 3 corrigés (graine de `getProperties()`, message `TDT_READ_ONLY` du pont, commentaires périmés). **3 refusés avec motif** :
     - *Format de date imposé à l'écriture* : écart déjà assumé (décision de conception n°2 du Plan 3), pas un défaut nouveau.
     - *Les lectures ne prennent pas de verrou* : prendre le verrou en lecture ajouterait 5 requêtes à CHAQUE lecture, soit plus que tripler le coût d'un sondage de tchat, sur un quota de 60 lectures/min. Le prix d'une lecture déchirée est un affichage brièvement incohérent qui se répare au sondage suivant — mauvais échange.
     - *`apiGetChangelog` refrappe GitHub à chaque appel* : c'est un appel sortant vers GitHub, il ne consomme aucun quota Sheets, et il n'a lieu que quand quelqu'un ouvre le changelog. Un cache inter-requêtes y ajouterait un risque de péremption sur la page dont le seul but est de montrer la dernière version.
- **Copie de test — valeurs utiles** : classeur `1IpnM_k7sicaktxtvwaYMiMnFbnJz6nhsELmagF1WS78`, compte de service **Éditeur** depuis le 2026-09-21. Les deux joueurs factices `Licorne` et `Chevre` ont le mot de passe `3` (colonne D de `Players`). Onglets techniques désormais réellement présents : `ScriptLock`, `ScriptProperties`, `Idempotency`, plus `Notes` auto-créé avec ses en-têtes canoniques.
- **Vercel — ne JAMAIS connecter le repo GitHub au projet Vercel** : déploiement par CLI uniquement, `vercel deploy --prod --yes --scope troispiliers` (sans `--scope` : « Not authorized »). `.vercelignore` limite l'envoi au code servi.
- **Vercel — points ouverts** :
  - Cache inter-requêtes toujours inexistant (chaque appel relit les onglets qu'il touche) → tuile « efficacité du cache » à 0.
  - **Trois** onglets techniques (`ScriptLock`, `ScriptProperties`, `Idempotency`) apparaîtront dans le classeur — ne pas les supprimer à la main. `Idempotency` s'auto-élague à 200 lignes.
  - Une date écrite impose le format `dd/MM/yyyy HH:mm:ss` au lieu d'hériter du format de colonne (écart assumé, décision de conception n°2 du Plan 3).
  - `startRow` de `lib/sheets-read.js` vaut 1 quand la ligne 1 n'est pas un en-tête (héritage du Plan 1, non traité).
  - Pas de limite de durée d'exécution interne : un `fn.apply` échappe au `timeout` de `vm`, seul `maxDuration` borne.
  - Fixture de parité avec de vraies cellules date à ajouter.
- **Piège shell** : les heredocs Git Bash sur cette machine mangent un niveau d'antislash — un `\'` écrit dans un heredoc arrive sans échappement et casse le fichier généré (constaté deux fois de plus cette session). Passer par un fichier de script écrit avec l'outil d'édition.
- **Cross-realm dans les tests `vm`** : `assert.deepStrictEqual` / `instanceof` échoue sur des objets construits dans un sandbox `vm` différent. Préférer `assert.deepStrictEqual({ ...valeurVm }, attendu)`. Ne jamais modifier une aide de test partagée pour contourner ça (fait puis reverté deux fois : v3.14, v3.32.0).
- **Piège de spécificité CSS (v3.32.0-v3.32.2)** : `.foo` (0,1,0) ne bat jamais `input[type="date"]` (0,1,1). Utiliser `.wrapper .foo` ou `input.foo`.
- **Environnement de prévisualisation** : le pane intégré peut rapporter `window.innerWidth === 0` juste après un `preview_start` — appeler `resize_window` avant toute lecture dépendant de la largeur.
- Garde-fous en place : `tests/mobile-audit.test.js`, `tests/papercuts.test.js`, `tests/guide-audit.test.js`, `tests/dropdown-outside-click.test.js`, `tests/cache-bytes.test.js`, `tests/innerhtml-audit.test.js`.
- **Modification fantôme récurrente de `context.md` — 3 occurrences les 2026-09-17/18** (section « §9bis — sélection du modèle pour les sous-agents »), jamais commitée, origine inconnue. `git status` systématique en début de session et avant tout commit. Non réapparue le 2026-09-20.
